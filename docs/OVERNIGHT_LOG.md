# Overnight log — Sprint 0 → Sprint 2, night of 2026-09-01

Running handoff. Newest at the bottom. Read the **Blocked on you** section first.

## Blocked on you (morning list)

1. **ScrapeCreators credits: balance is 0.** Every live read, fixture recording, the social-URL reliability spike and the 20-clip watch probe are blocked. $47 buys 25,000 credits at scrapecreators.com. Until then, wrappers and fixtures are built from the vendor's OpenAPI examples and marked `fixture: spec-example`; swap to `fixture: recorded` after purchase (one script: `npm run fixtures:record`).
2. **Telegram bots.** Tonight uses the local test bot `@mayatesstteest_bot` (the token in the legacy `.env.local`) for the new dev deployment. Create two more through BotFather for `creator` (staging) and `creator-main` (prod) when you're up.
3. **Clerk.** Tonight reuses the legacy `pk_test`/`sk_test` development instance. A production instance is needed before Sprint 3.
4. **Google Cloud.** An OAuth client already exists (localhost redirect). It needs the staging/prod redirect URIs added and the sensitive-scope verification video submitted.
5. **Meta developer app + Business Verification** for the Instagram App Review (6–8 weeks; the long pole). **Apple Messages for Business** application through an MSP.
6. Stripe products/prices for §19 can be created by API in test mode tonight; **live-mode** products need you.

## Guardrails held tonight

- Nothing touches `main`, `staging`, the production Convex deployment, or the production Telegram bot.
- Vendor spend cap ≈ $25, itemised below. (ScrapeCreators spend is $0 tonight by necessity.)
- No secrets committed. `.env.local` in the worktree is gitignored; keys copied from the legacy tree by hand.
- Commits go straight to `creator` tonight; every commit message says what and why.

## Environment facts verified at 2026-09-01 late evening

| Thing | State |
|---|---|
| `gh` | logged in as jcastro506 |
| `vercel` | logged in as jcastro506 |
| Convex CLI | logged in (`~/.convex/config.json`) |
| Gemini key | live; `gemini-3.1-flash-lite`, `gemini-3.5-flash`, `gemini-3-flash-preview` visible; 3.7 to be confirmed |
| OpenRouter key | live |
| ScrapeCreators key | live, **0 credits** |
| Telegram | local token = `@mayatesstteest_bot` (test bot) |
| Stripe | `sk_test_…` (test mode), webhook secret present |
| Clerk | `pk_test_…` (development instance) |
| Google OAuth client | exists, localhost redirect only |
| Zernio key | present (not exercised tonight; Sprint 4) |

## Git

- `legacy` branch + tag `legacy-2026-09-01` = today's `main` (`347899e`).
- `creator` = orphan branch, worktree at `../heymaya-creator`.
- `codex/creator-sprint-plan` = the plan on the legacy tree (for the PR record).

## Timeline

- 23:xx — decision: same repo, orphan branch. Worktree created. Four salvage audits running (web/infra done; maya/agents, integrations/schema, tests/scar pending).
- 20:44 — New Convex project **heymaya-creator** (team castrojoshua805), dev deployment `impressive-roadrunner-997`. Created with `arch -arm64 npx convex dev --once --configure new` (the Rosetta esbuild mismatch from the legacy repo applies here too: always `arch -arm64 npx convex …`). A separate **staging** project per §20 is still to be created.
- 20:50 — Scaffold committed on `creator`: Next 16.3 + Convex + Clerk + Stripe + PostHog + Zod; vitest + convex-test; deploy guards; CI. Dev keys copied from legacy `.env.local` with a real parser (the naive `source` echoed a Fly token into a shell error — rotate `FLY_API_TOKEN` for the retired product as hygiene). Server-side keys set on the dev deployment via `convex env set`.
- Confirmed `gemini-3.7-flash`, `gemini-3.6-flash`, `gemini-3.5-flash`, `gemini-3.1-flash-lite` are all available on the key.
- ScrapeCreators endpoint additions for the doc after reading the full catalogue: **TikTok collection videos** (a creator's public collections = their own swipe file) and **Instagram story highlights** (what they choose to keep on their profile), both 1 credit at onboarding → dossier `interests`.
- 21:1x — ScrapeCreators wrappers pruned to the plan's set and extended: TikTok +`searchSuggestions`, `profileRegion`, `collectionVideos`, `popularCreators` with filters, `searchTop` with time/sort, `songVideos` cursor; retired `popularVideos/Hashtags/Songs` removed; `extractClipId` reads `music.id_str` and refuses unsafe numeric ids (scar tissue: TikTok ids > MAX_SAFE_INTEGER). Instagram +`search`, `searchPopular`, `searchProfiles`, `searchHashtag`, `reelsTrending`, `profilePostCount`, `highlights`, `highlightDetail`, `audioReels`; `profile`/`mediaTranscript` always pass `cache_max_age`; `post` takes a URL. New `platforms/cross.ts`: `findSocialProfiles`, `redditSearch`, `creditBalance`, `dailyUsage`, `mostUsedRoutes`, and `CREDITS_BY_PATH` (the endpoint cost table that `costEvents` reads; no endpoint without a cost). tsc clean, 12 tests green.
- Two audits (maya/agents, tests/scar) were killed by the session rate limit (resets 12:40am ET); relaunched. If they die again I finish them by hand after the reset.
- **Claw Messenger** (clawmessenger.com), asked about by Josh: an iMessage/RCS/SMS relay API, no Mac required, $5–50/mo by message volume, Agency $199/mo per subtenant; sends over a persistent WebSocket, inbound as WebSocket events (webhooks only for "approved managed setups"); an "agent line" can text a new number first (auto-registers it); registered-number caps per plan (1/5/20/50); throttles new-recipient conversations with 429s; no mention of Apple approval. The legacy product used it via an OpenClaw channel plugin and hit a real defect: the shared key is single-tenant, so a second creator's socket stole inbound and routed it to the wrong creator (observed 2026-05-16); isolation needs one tenant + key + route per creator via their `/api/tenants`, `/api/keys`, `/api/routes`. Verdict in the reply to Josh.
- 08:0x (machine clock) — **All four salvage audits done → `docs/SALVAGE_MANIFEST.md` (1,296 lines).** Part 4 has the numbered scar-tissue list (30 items with the guard each needs); Part 1 lists 50 more with file:line. Two items have no guard anywhere today: TikTok ids > MAX_SAFE_INTEGER (now guarded by `extractClipId`) and the half-dead ScrapeCreators cache (now replaced by `read()`).
- **`read()` cache layer live in code:** stable keys, in-flight claim (5 concurrent reads → 1 vendor call, tested), TTL per kind, `costEvents` per call. Finding: ScrapeCreators responses carry `credits_charged` (37 of 39 spec examples) → cost is **vendor-reported** when present, endpoint table otherwise. Spec corrections: Instagram comments cost **15** credits, Find Social Profiles **10** (both were 1 in the plan; fixed in §12.1).
- Legacy TikTok search option types were wrong (`this_week`, `likes`); replaced with the spec's `this-week`, `most-liked`, `date-posted`.
- Next: port the core PORT modules (llm, jobs, messages, plainLanguage, spendCeiling, breaker, directives, benchmarks, quality) adapted to the new schema; then Telegram webhook/pairing; then onboarding ingest.
- 08:1x — **Core modules ported and green: 180 tests, 0 type errors.** `convex/core/`: llm (with `costs.record` replacing the Fly-era cogs hook), jobs, messages (drafts hook removed; own-handle carve-out for the leak guard), pairing (Fly image tests dropped), plainLanguage, directives (legacy account→customer double lookup collapsed), breaker, quality, embeddings, delivery, cadence (timezone helpers only), plus founderDay/llmBudget tests. Schema aligned to the proven modules' field and index names (`messages.ts/body/awaitingAnswer/deliveredAt`, `jobs.deadlineAt/updatedAt/status succeeded`, `directives.verbatim/active/supersedesId`, `creators.pairingToken/telegramChatId`). Tests seed through one helper (`tests/lib/creatorRow.ts`) so a schema change is one edit.
- Parked in `pending/` (excluded from tsc/vitest) for adaptation: benchmarks (nicheCache→readCache), spendCeiling (planFeatures/gtmAuditEvents), cadence.test, modelSwap.test (needs directiveGate/publishDecision shapes), embeddings.test (buyerMap), sweepMapIsSingular.test (needs watchers).
- 08:3x — **Telegram layer adapted and green (183 tests).** `integrations/telegram/client.ts` ported and extended: inline-keyboard `buttons`, `message_reaction` updates, `answerCallbackQuery`, `setMessageReaction` (bot-side reactions, §21.5). `core/telegram.ts`: delivery as a job with `deliveredAt`/`deliveryError` proof, typing before slow work, `update_id` dedupe, inbound → `converse` job (no machine hand-off). `core/telegramFiles.ts`: oversize refused before the fetch, bytes to Convex storage, row + job. `telegram/webhook.ts` + `http.ts`: secret check, pairing with a talk-back on failure, first_read job on pair, buttons acked immediately, reactions recorded, unknown senders get the signup link. Pairing now also sets `channel.paired`.
- 08:5x — **Dispatcher + first real turns (185 tests).** `core/scheduler.ts`: `HANDLED_KINDS` = deliver_message · converse · first_read · ingest_catalogue, with a static test that every enqueued kind has a handler and every handler a producer. `crons.ts`: drain every minute. `agent/soul.ts` (§21 verbatim, versioned), `agent/registry.ts` (§4; writer google/gemini-3.7-flash via OpenRouter tonight, screener/critic z-ai/glm-5.3-flash, watch gemini-3.1-flash-lite direct), `agent/context.ts` (stable prefix → suffix, `produced` stamp), `agent/converse.ts` (minimal real turn; classifier/tools/critic are Sprint 3), `onboarding/firstRead.ts`, `onboarding/ingest.ts` (catalogue via `read()`, baseline + multiples, §13.1 sample, transcripts for the sample, one writer call → Zod-validated dossier → `first_read`). `contracts/dossier.ts` is the §14.1 schema. Watching (Gemini) is the next block.
- 08:2x — **First live run on the dev deployment (`impressive-roadrunner-997`), zero ScrapeCreators credits:** `onboarding/dev:seed` → `ingest_catalogue` job → fixture reads through `read()` → 1 own post (the spec example) → one writer call through OpenRouter ($0.0087, recorded in `costEvents`) → dossier JSON came back and **failed the §14.1 contract on `persona.onCamera`** (the model said "unknown" because it only had captions). Contract widened to allow `unknown` on register/onCamera and a few interest sources; job retried by the minute cron. The test bot `@mayatesstteest_bot` webhook now points at the dev deployment with `message_reaction` enabled.
- Added `onboarding/start.ts` (web entry: `start`, `describe`, reactive `progress`) and `onboarding/dev.ts` (`seed`, `status`, `retryNow` for `npx convex run`).
- 08:27 — **First message delivered to Josh's Telegram** (test bot, dev deployment, seeded creator "leahruns" on fixtures). Chain verified live: seed → ingest (fixtures) → dossier v1 (validated) → first_read → deliver_message → `deliveredAt` set. Spend for the whole run: $0.028. `onboarding/dev:pairChat` binds a chat without the web flow (operator smoke only).
- 09:2x — **Watch pass verified live, including the degrade path.** With the TikTok URL rebuilt from handle+id, the transcript read returns the fixture WebVTT; the watch fetches the post's `videoUrl` (the spec example's signed CDN link, long expired) → `media fetch 403` → a `read`-depth card with `degradedBecause` and no visual claims, exactly §14.2. Real watching needs real CDN URLs, i.e. ScrapeCreators credits. Own posts are now unique per creator (`by_creator_post`); unpaired deliveries are deferred without burning attempts and woken on pairing.
- 09:3x — **Sprint 2 core in code: sampler, gate, scout.** `scout/sampler.ts` (fleet job every 6 h: one read per distinct admired account, observations, per-account baseline, `signals` rows for the admirers), `scout/gate.ts` (rails: plan status, paired, quiet hours in the creator's tz, daily cap 3, open question; then the top-10 candidates), `scout/scout.ts` (the skill: notable + fit → one message with the link and shot list / not me / save buttons; every other candidate gets a verdict and a reason), `config/thresholds.ts` (every tuned number, versioned). Idea buttons handled in `converse` (not me → passed; shot list → a short writer call). Crons: sampler 6 h, scout hourly at :05.
- **Live on dev:** a second seeded creator with three admired accounts → first read delivered to Josh's phone; sampler wrote 1 signal (fixtures, `SCOUT_DEV_RELAX=1` on dev only); scout refused with `quiet hours` (creator tz America/Los_Angeles, 06:37 local). The 07:05 PT cron should send the scout message unprompted. That is the first proactive message.
- Local toolchain: this Mac's `/usr/local/bin/node` is an x64 build under Rosetta while npm resolves optional native deps for arm64, so vitest (rolldown) and `next build` (tailwind oxide, lightningcss) can't find their binaries after a clean install. Fix in progress: install the x64 optional binaries alongside (`npm install --cpu=x64 --os=darwin --force`). CI on Linux is unaffected. Morning option: install an arm64 Node 22 via nvm and use it for this repo.
- 09:5x — **Sprint 2 signal sources complete in code:** `scout/sweep.ts` (daily, one search per distinct lane keyword across creators, TikTok keyword + Instagram reels search → observations tagged by keyword → `shape` signals) and `scout/readback.ts` (daily own-post refresh, multiples, `win` signals at ≥3×). Scout skill now distinguishes breakout / shape / win. Live on dev: sweep over 8 keywords wrote 2 shape signals; the seeded creator now has 2 candidates waiting on quiet hours.
- Toolchain resolved for tests: vitest 4.1 pins rolldown 1.2.7 whose darwin-arm64 binding is not on the registry (latest 1.2.6) → **vitest 3.x**; 185 tests green locally. `next build` still fails on this Mac (Turbopack evaluates the Tailwind oxide binding as darwin-x64 while node reports arm64); **CI now runs `npm run build` on Linux** and CI typechecks against the committed `convex/_generated` (codegen needs a deployment).
- The legacy repo's `.claude/launch.json` got an uncommitted `creator-web` entry (runs the worktree's dev server on :3001) so the Browser pane can drive the new onboarding; revert or keep, your call.
- 10:00 — **The critic is live** (`agent/critic.ts`, z-ai/glm-5.3-flash, a different family from the writer): every scout message and first read is judged for slop, invented numbers, leaks, voice, safety, missing link or action, house-rule violations and length; one rewrite, then drop with the verdict on the signal. A skipped critic (vendor down) flags the row `criticSkipped` rather than passing silently. The Browser pane now drives the new onboarding at http://localhost:3001 (Clerk dev instance, sign-up form renders).
- 10:07 — The 07:05 PT scout cron skipped the seeded creator by design (`dueForScout` waits for 08:00 local; the gate itself opened at 07:00). Running the scout by hand for the live check instead; the 08:05 cron covers it from tomorrow.
- **Blocked on a human:** the Clerk dev instance shows a Cloudflare "verify you are human" step on sign-up, which I will not solve. The onboarding walk in the Browser pane stops at that step. Options for the morning: turn off bot protection for the development instance in the Clerk dashboard (Configure → Attack protection), or sign up yourself at http://localhost:3001/sign-up.

## 10:10–13:30 PT — the thin UI, the calendar, and the taste layer

- **Thin UI (S4)**: `/app/today`, `/app/ideas`, `/app/lane`, `/app/settings` under one bottom nav; every read is a Clerk-scoped query in `convex/ui.ts`; the writes are settings, a verbatim correction (→ directive), revoke rule, pass idea, "posted it", the admired list, the calendar picker. CI had failed four pushes because the copy guard counted `from "convex/react"` as a vendor name in the UI; the guard now reads visible copy only (imports, comments, providers and API routes excluded). CI green from `bbaee6f`.
- **Calendar (Sprint 3)**: Google OAuth from Settings (state token = auth, single use, 15 min), AES-GCM bundle in `connections.tokenRef` (fresh `ENCRYPTION_KEY` on the dev deployment, not the legacy key), 30-minute sync of the next 14 days as five fields, code-then-screener classification, private events keep no title, `calendar` signals for filmable events ≥ 2 days out, the gate's calendar rail, the scout's fourth kind proposing ONE filming block, `block it / idea only` buttons, `recordExternal` refuses without `consentAt` (named test), move/delete follow, disconnect revokes and purges. **Deviations logged**: both scopes at connect (not incremental); polling, not push channels, for the pilot. **Operator**: the local redirect URI is `http://localhost:3000/api/google-calendar/callback` (already on the Google allow-list from legacy); run the creator app on :3000 to try it, and add the production URI when the domain exists.
- **Taste (§13.10, new in the plan)**: Josh asked for the personalisation architecture. Designed and built: `tasteEvents` rows written by code at the moment of each reaction/tap/reply/post/silence; features named by the writer in the same call that writes the idea; affinities computed and decayed (45-day half-life) with `n` as confidence; a weekly prose taste note in the prefix between dossier and house rules; the gate re-ranks by affinity and rail-drops a hard no (format/topic/sound/account only, never source) with the reason on the signal; the explore slot (1 in 5) keeps taste from collapsing; Settings shows it with correct-me; Ideas shows reactions and "posted it". 209 tests green.
- **Not yet**: the `match-post` skill (posted is self-reported from Ideas until then), the nightly consolidation call (§15.7 layer 3), the weekly review's "liked vs worked", reply-hour cadence learning, the admired-list "stop watching @x?" question after three passes.

## 13:30 PT — opinions live

- Inbound routing is code (`convex/agent/inbound.ts`): commands (stop / resume / forget that / talk to a person / delete) are regex-matched and enforced by rows; links parse to platform, post id, handle and "their own"; files route by MIME.
- The opinion path is live on dev: a fixture TikTok link sent from the paired chat came back as a grounded read citing their own WNBA post's 559,925 views, confidence "solid", with a `predictions` row (expected 1.3× per §13.6). First attempt failed the citation check twice and she said so in words; the fix was the reasoning-token budget (memory 2026-08-09: thinking bills to max_tokens) plus treating an answered turn as done so the queue does not retry a turn she already answered.
- Screenshots are read into labelled numbers and voice notes transcribed by Gemini (no new vendor; Groq is not in the stack yet), then answered as a text turn. Untested live: no image or voice fixture can be injected from the CLI. **Operator**: send the test bot a screenshot and a voice note once, and a draft video under 20 MB.

## 13:35–13:45 PT — match-post, the weekly review, the dossier rewrite

- **match-post (§13.5)** runs after the daily readback: each new own post is judged once against the last fourteen days of ideas; certain/likely → posted + the strongest taste event scaled by the post's multiple; unsure → "is this one from the … idea?" with two buttons; no → a negative example, taste untouched.
- **Weekly review (§11.2 #14)**: Sunday 09:00–20:00 on the creator's clock, hourly check, once a week. Rung computed by code (§13.7), liked vs worked, experiment ledger on `creators.experiments[]`, prediction track record once three are scored, then the three-question pilot check-in as the one open question. Ran live for the second seeded creator: "zero posts this week against the one planned, so we're sitting on no new numbers…" — grounded, honest, thin-week branch.
- **Prediction scoring (§13.6)** after each readback against the 48 h sample; drafts score against the first own post after them; other people's links never score.
- **learn-creator weekly (§15.7)**: the onboarding synthesis is now `ingest.synthesize`, re-run 60 s after each review and 5 min after a Settings correction, with the previous dossier, house rules, notes, taste and posted ideas in front of it; `writeDossier` keeps `dossierPrevious` and names the changed sections in `dossierDiff`.
- **Dev-data smell**: both seeded creators are paired to the operator chat, so inbound from that chat routes to the first one by index. Harmless in dev; production pairing is one chat → one creator by construction (`pairing.claim` re-pairs). **Operator**: nothing to do.

## 13:45–13:55 PT — remember, deletion, billing

- **remember**: after each text turn the screener quotes (never infers) a note or a house rule from what they said; duplicates confirm instead of repeating. Settings shows both.
- **Deletion (§16.5)** is one procedure from Settings after a typed DELETE and the export offer: freeze → Stripe cancel (customer retained) → calendar revoke and purge → the final Telegram text → every row in every creatorId table (a test checks the list against the schema) → files → Clerk user via the web route. **Operator**: PostHog person deletion and the receipt email need a personal API key and an email vendor; both are on the list.
- **Billing (§19)**: checkout, portal, webhook on the Convex site (public, signature-verified, idempotent, out-of-order safe), one Maya line per state change, the day-5 trial text, past-due grace in the gate. Stripe **test** product "Maya" with four prices (founding $19/$180, list $29/$290) and a test webhook endpoint `we_1UBILPQbBBmG0KdAKxZjIxi2` → dev; ids and the signing secret are on the dev deployment. **Not verified live**: a real checkout needs a signed-in browser session, which the Clerk CAPTCHA still blocks for me. **Operator**: run through checkout once with a Stripe test card (4242…) from Settings; then, for production, run `node scripts/stripe-products.mjs` with the live key, add the live webhook, and set the six env vars on the prod deployment.

## 13:55–14:00 PT — landing, legal, ops console, consolidation

- Landing page (S1) with the phone mock, three beats, one price, "what she can't do", FAQ; Privacy and Terms drafted (**Operator**: a person reviews both before launch; set the **18+ age gate in Clerk**; the data-deletion URL for Meta/Google review is `/privacy` + Settings). PostHog landing events are not wired (no key on the creator app yet).
- `/ops?token=…` operator console (token on the dev deployment as `OPS_TOKEN`; fail-closed). Nightly consolidation tombstones expired notes and learns the reply hour; the scout aims for the hour before it. "Stop watching @x?" after three passes, answered by buttons.
- 252 tests, CI green through billing.

## 14:00–14:05 PT — "why is @x growing", first week, save

- profile-creator on demand: "why is @stoolpresidente growing" came back in four lines, live. **Fixture caveat**: with zero ScrapeCreators credits the fixture client answers every handle with the same spec example, so the numbers she cited were the seeded creator's own post. Real credits make this real; the skill and routing are done.
- The first week is a schedule row (`creators.firstWeek`); the day-4 invitation to send a draft is hers to initiate. "save" keeps the idea (`savedAt`) and Ideas can filter to saved.
- Plan §6 now opens with a status block: built · not built · operator-gated. 255 tests, CI green.

## 14:05–14:20 PT — Zernio connections

- Read half ported (`convex/integrations/zernio/index.ts`, `convex/connections/zernio.ts`): one profile per creator persisted before the redirect, OAuth kickoff from Settings (paid plans only), webhook `/zernio/webhook` (HMAC over the raw body; 401 on a bad signature, verified live), reconcile on return, needsReconnect drives state, deletion removes accounts then the profile. `probeAnalytics` records the analytics shape into `vendorHealth` before anything depends on it.
- The dev webhook subscription was sent to Zernio with a fresh `ZERNIO_WEBHOOK_SECRET` on the dev deployment; Zernio's create response carried no id, so the listing call below is the check. **Clarified for Josh**: Fly is not in this stack; the `FLY_API_TOKEN` item is only a legacy secret to rotate because it was echoed into a shell error overnight.
- **Operator**: connect one throwaway TikTok or Instagram account from Settings (Zernio's OAuth screen needs a human login), then run `arch -arm64 npx convex run connections/zernio:probeAnalytics '{"creatorId":"…"}'` and read `vendorHealth`. That result decides how own-account numbers merge into `ownPosts` with the `zernio` source label.

## 14:20–14:30 PT — investigation (§13.11) and memory recall

- Josh: the agent must use the whole ScrapeCreators catalogue intelligently, multi-step, when a post is worth a second look. Built as §13.11: a tool belt (post_info, post_transcript, post_comments, sound_info, sound_videos, profile, account_posts, search_keyword, search_hashtag, own_rhymes, calendar_upcoming) executed only through `read()`; a bounded loop (6 calls / 40 credits / 60 s by default, budget shown to the model every turn); results summarised by code; every call a trace row on the signals judged (scout) or the prediction (opinion). Wired into the scout, opinions on links (4 calls) and profile-creator. OpenRouter client and callModel gained tool calling.
- **Verified live**: "is this the sound or the guy?" on a fixture link → she called `post_info` ("check video stats and sound id to see if it's the account or sound") and answered "it's the guy and the interruption premise, not a trending sound carry", with a citation and a `weak` confidence. The scout, given two obviously poor candidates, made no lookups and dropped both, which is the right call.
- Memory recall (§15.7 layer 4): `memories` with a 768-dim vector index; saved ideas, sent ideas and notes are embedded; "what was that idea about…" runs the search and the passages ride into the turn.
- Shell note for future me: never put backticks inside a `git commit -m "…"` string; zsh runs them.

## 14:30 PT — the belt covers the catalogue; playbooks in every skill

- Josh: "a smart agent armed with skills and clear prompting is essentially what we want" — yes, plus code-enforced promises and the learning loops. Nine more tools (Instagram reels/hashtag/popular, reels on an audio, both trending feeds, suggestions, discover creators/profiles). Every judgment skill carries a lookup playbook (`convex/agent/playbooks.ts`): the questions to answer with tools, which tool, when not to look. Explain-post and the weekly review look up too.
- Multi-step, live: "what's @stoolpresidente doing that's working, and is any of it mine" → three writer turns in the ledger (two tool rounds, then the answer), a four-line read ending with "not yours: the loud barstool-style street interview setup". Reads went through the fixture client, so no vendor credits.
- Noticed in the ledger: the critic's primary (GLM 5.3 flash) logs $0 and the fallback (DeepSeek) runs every time. Probing the model id directly next; if the primary is dead on OpenRouter the registry needs a different second-family critic.

## 14:35–14:45 PT — Sprint 3c evals; intent by the model

- Josh asked whether there is a sprint for evaluating the whole complex loop (watch, decide, check trending, relate, idea, message) so she is never corny or bad. §17 named the golden sets; the harness did not exist. Sprint 3c is now in the plan and built: deterministic checks (claimed numbers must be in the evidence, the short exact-tell list, leak, length, one question, no bullets, name once, scout has a link), a second-family judge (corny / generic / flattering / tool-speak / specific / would-send, 0–3), scout dry runs (message + trace, no rows), a nightly suite over real outbound, a scout suite for prompt/model changes, and 👍/👎 labels on the ops console that become the golden sets. Judge agreement with labels appears after five.
- Josh: "our LLM should be intelligent enough for many jobs" about regex. Agreed and applied: the two intent detectors are now one classifier call per text turn; code keeps only commands (a "stop" must never be misread), links, files, the private-calendar floor, and budgets. The eval tell list stays deliberately short (§21.4) and the judge does the real work.
- Found via the ledger: the critic's primary (GLM 5.3 flash) had been falling back on every call because thinking ate its 400-token budget; listed as a reasoning model, it now answers.
- Evals live: the `recent` suite judged real outbound; the judge independently called the removed check-in "needy" and caught first-night messages that trailed off (cause: reasoning tokens eating a 500-token budget; fixed by listing Gemini 3.x for the allowance and giving the first read 900). The scout dry-run suite runs (three lookups on a seeded breakout, then "nothing worth their time" because fixture content is prank clips, not running). **The scout suite produces judged messages once real credits exist**; the machinery is verified.

## 14:55 PT — transfer: formats travel, topics don't (§13.12)

- Josh: content outside the niche is still stealable (a format, a trend's shape, a sense of humor, an angle). The scout had been judging fit as topic fit (the fixture prank clip was dropped as "outside the running lane"). Now: two kinds of fit, either carries; a rejection must say whether the topic or the format was the problem; a transfer pick names the source as not their world, what transfers, and their version on their subject.
- Daily format watch (`convex/scout/formats.ts`): both trending feeds once, fleet-wide; the screener keeps the formats worth stealing; one `worth_seeing` per kept format per creator with a fingerprint; the same shape twice a week is not news; the gate passes one marked worth_seeing a day and none unmarked. On fixtures the trending feed has one post and the screener kept none; real credits make it real.

## 15:05–15:15 PT — hyper-personal prefix; management in plain words; a fabricated action caught

- Every prefix now carries their last six posts with numbers and their next seven days (titles only, never private), so every skill judges against their own work and life.
- Management intents from the classifier write the same rows as the Settings controls (quiet hours, tone, niche, add an admired account with the shared rule, stop watching with confirm buttons); a chat-completeness registry test maps every web control to its chat path. Live: "can you not text me before 9am" → quiet hours 22:00–09:00; "be blunter" → blunt; "add @runwithcarly" → the row, after the fix below.
- **Caught live**: the first "add @runwithcarly" was classified as chat and the writer answered "added, tracking her now" with no row written. Fixed three ways: the converse skill may never claim an action (it says what to text so it lands), the classifier carries worked examples, and the recent eval suite flags any plain reply that claims added / set / done without a management row. This is the exact failure class evals exist for.

## 15:10–15:20 PT — in the moment (Sprint 3d)

- Josh: real use is "I'm here, I like this place, want to make something", with a photo or a clip; she must handle it and do everything on the fly, full CRUD. Built: Gemini classifies a file (screenshot / draft / scene) so a photo of a place is never read as analytics; the moment skill reads the scene against who they are, their taste, their week and their calendar → two angles, one recommended, as an idea row with buttons (shot list · block time now · save); classifier intents moment / edit_idea / drop_idea for text; edits change only the named field.
- Live (text): "tiny ramen place after my long run, soaked, whole run club here" → "post-rain ramen is gold. don't stage anything… do the gear autopsy, you're literally dripping wet, so the proof is right there." Personal to a gear-review runner, not a food clip.
- Live (photo): a scenic overlook photo through the moment path → Gemini read the scene, she proposed "accidental elevation gain as a late starter" over "inspirational running footage" because it "fits your dry tone"; edits landed ("make it 15 seconds", a new hook), block-now proposed a block fifteen minutes out. Cost ≈ 1.1¢ for the whole turn.
- Conversation is armed: a free-text "is #marathontraining dead" ran three writer turns (lookups) and came back with a grounded take. Recall is in the belt. The classifier decides every intent; regex is gone from intent routing.

## 15:30 PT — the vendor's own docs

- **Credits**: the ScrapeCreators key on the dev deployment (ends …xTL2, the same key as the legacy prod deployment and the legacy .env.local) answers `creditCount: 0` from `/v1/credit-balance`. Every read still goes to fixtures. **Operator**: check the dashboard for that key; if there is a second key or a top-up, set `SCRAPE_CREATORS_API_KEY` on `impressive-roadrunner-997` and flip `SCRAPE_FIXTURES` off.
- Read the vendor's agent skill and the 13 research skills in github.com/ScrapeCreators/social-media-research-skills, plus the per-endpoint OpenAPI (`docs.scrapecreators.com/{path}/openapi.json`). Useful in three ways, all applied: (1) real credit costs (post detail 10 when media found, IG comments 15, find-social-profiles 10, profiles free from the vendor cache) → the belt, the ledger table, a checked-in `docs/scrapecreators-credits.json` with a test; (2) a path we used is gone from the spec (`/v1/instagram/user/posts` → `/v2`); (3) their workflow rules (median baseline, under ten posts low confidence, 5×/2×/1.5× labels, regional trends, comment buckets, transcript segmentation) folded into the playbooks. Their skills are research reports for an agent with a shell; ours are the same moves inside a bounded, budgeted loop with a personal prefix, so we borrow the rules, not the format.
- 283 tests.

## 15:45–16:05 PT — sounds, analytics, onboarding screens 5–7

- Sound signals: observations now carry the clip id; a daily pass makes a `sound` signal from any sound two or more lane accounts used this week; the gate's sound rail; the scout's sixth kind looks the sound up first.
- Landing analytics (view, scroll depth, CTA click; memory persistence, nothing personal). The calendar round trip can return to onboarding. Onboarding: screen 5 calendar (skip allowed), screen 7 done (timezone confirm, quiet hours, progress from jobs, Open Maya in Telegram), existing account → Today, tap-to-pick suggestions (popular creators in their band and country, accounts they follow). 285 tests.

## 18:00 PT — budgets as rows, vendor smoke, alerts, retention

- Daily budgets (§3): every priced cost event lands on the creator's day in their timezone; the gate refuses proactive at the day's spend, watch or credit cap and says which; replies are never throttled. Caps in thresholds ($0.75, 8 watches, 60 credits; tune from the ledger).
- Vendor smoke (§17.1): one free call per vendor daily into vendorHealth, on the console; ran live: ScrapeCreators, Telegram, OpenRouter, Gemini all ok (the vendor's balance call answers even at 0 credits).
- Operator alerts (§16 principle 5): hourly, one Telegram message to the operator chat for new dead jobs, outbound undelivered over an hour, failed smoke, connections needing attention; ids only, never content. Retention (§16.5): messages 12 months, calendar fields 90 days rolling, expired oauth states, nightly and bounded.
- Creator-facing status (§7 S3): "behind today" once when her own work died and nothing reached them for six hours; "couldn't see TikTok today" when the vendor smoke failed; never at night, never against the cap. Paid promotion was already filtered at the sampler and sweep; toxicity rides on the critic's `unsafe` label. 295 tests.
- Lane benchmarks adapted from the parked legacy module (guards kept: fewer than eight posts or three accounts is unusable, never a number); in the review, on Results, and a free `lane_benchmark` tool. Salvage manifest addendum: spendCeiling and liveness/watchers are superseded by budgets, alerts and status. Offline banner on the thin UI. 297 tests.

## 18:15 PT — metrics, COGS, the fixture recorder, creator-main

- `/ops` now carries §18 metrics (activation, weekly active, the north star, track record, silence quality, funnel p50/p95) and §3.6 COGS from the ledger (per creator per month, margin at $19 and $29, by vendor). Live on dev: 6 creators, first message p50 4 min, $0.26/creator/month on fixtures (real reads will move this; the $8 pilot estimate in §3.6 stands until credits exist).
- `npm run fixtures:record` records real vendor responses through the dev deployment (one call per path, sample queries) into fixtures.recorded.json; run it once credits exist, then set `SCRAPE_FIXTURES=recorded` where replays are wanted.
- §20 branch model: `creator-main` created from `creator` as the release branch (CI runs on both). **Operator**: the staging Convex project (dashboard: new project `heymaya-creator-staging`, a deploy key into the repo's secrets as `CONVEX_DEPLOY_KEY_STAGING`) is the missing half; until then `creator-main` is a branch, not a deployment.
- Cost reconciliation (§16.4) daily against the vendor's per-day usage list (`/v1/account/get-daily-usage-count` answers `[{usage_date, total_credits, request_count}]`); live: vendor 0 vs ledger 0 today, agree. Rollback script (`scripts/rollback.sh <sha> dev|staging|prod`) through the deploy guard. Registry test: critic family ≠ writer family, every role has primary, fallback and a sane budget. 301 tests.

### The fake model and the simulated day (§17.1–17.2)
- `MODEL_FAKE=1` answers every model call by purpose, deterministically, for free (`convex/core/fakeModel.ts`); the deploy guard refuses a shell with it set. Vendor reads stay on fixtures.
- `convex/__tests__/simulatedDay.test.ts`: breakout → gate → scout → idea row + message with the three buttons → signal verdict → the cap refuses a fourth send → a heart is a taste event and an affinity → the Sunday review writes the experiment ledger and no survey follows. Assertions on rows only.
- **It found a real bug on its first run.** `messages.send` accepted `kind`, `buttons`, `links`, `produced`, `awaitingAnswer` and `criticSkipped` and dropped all six before the insert, so every proactive message on dev was stored with no kind, no buttons and no links. Fixed; 304 tests.

### The simulated fortnight (§17.2) and the second bug it found
- `convex/__tests__/simulatedFortnight.test.ts`: a breakout a day for fourteen days, the scout daily, the review on both Sundays, the nightly expiry each night. Rows prove: never more than three proactive in a creator-day, nothing inside quiet hours, one open question at most, a dedupe key on every proactive row, verdict on every signal, zero model spend.
- **Second real bug.** With `send` now keeping `awaitingAnswer`, the open-question rail muted the scout for thirteen straight days after one unanswered "want the shot list?". Silence is an answer: an open question stops counting after `THRESHOLDS.openQuestionHours` (24) in the rail, and the nightly expiry closes stale ones. 305 tests.
- The fortnight now also carries one text a day from the creator through the classifier and converse (fourteen replies, none proactive, none on the cap) and a paired neighbour in the same lane who receives nothing (cross-tenant on rows).
- The simulated day also runs a link from the creator through converse to the opinion skill: a prediction row on the record, one reply, no proactive row, zero spend. 306 tests.
- Verified live on dev after the fix: a message sent through core/messages:send stored its kind, its dedupe key and its button row (message k57abjwxjnb8m0dtzmykrkd3ex8dmaxx). The seeded breakout on the dev creator was judged 'nothing worth their time today' by the real model, which is the same verdict as the dry-run suite on fixtures.

### Credits landed: real fixtures recorded, and the bug that would have hidden them
- 1,000 credits on key …xTL2. `npm run fixtures:record` recorded 16 live paths for ~15 credits. Trimmed for the repo (6 objects per list, 2 per scalar list, long strings clipped): 9.7 MB → 1.9 MB, vendor field shapes untouched.
- **Bug found before the fixtures could be used.** `SCRAPE_FIXTURES=recorded` labelled reads "recorded" while still serving the spec examples. A recording run would have changed nothing, and every read would have looked healthy against the wrong shapes. Recorded mode now loads the recording, falling back to spec per missing path, and is cached per mode.
- Recorder fixes: 64 MB buffer (three search paths blew a 1 MB pipe with ENOBUFS), and it takes a path list so a rerun is cheap.
- `/v1/tiktok/creators/popular` is **down at the vendor**, not here: 400 with "TikTok's Creative Center creator page/API is down", 0 credits charged. Dropped from the recorder's sample list; still in the belt for when it returns.
- New `convex/reads/__tests__/recordedShapes.test.ts`: 12 conformance tests over the real payloads. Ids, seconds-era dates (the 1970 trap), views, likes, authors on search results, the `aweme_detail` unwrap, transcript text, comment text. **All 12 pass** on first run, so the fixture-driven build held against reality. 318 tests.
- **Fixture/production mismatch, corrected.** The product sends `trim=true` on keyword search, hashtag search and the trending feed; `trim=true` returns each item FLAT (no `aweme_info` wrapper). The first recording sent no `trim`, so it captured the wrapped shape production never sees. The recorder now sends the same query the product sends, and those three paths were re-recorded. Verified against the live trimmed payload: 30 of 30 posts normalize with author, views and 2026 dates, so the normalizer's dual-shape handling is correct.

### First live creator, real vendor, fixtures off (2026-09-02)
Seeded @vanessaalopezz (running/travel, ~10 posts read) with two real admired accounts.
- **Third real bug, found only by running live.** The dossier came back truncated mid-object, twice: "dossier did not validate: JSON parse". The reasoning allowance was a flat 1,500 regardless of answer size, so a 3,000-token dossier had thinking eat most of the budget and the JSON stopped at ~4,600 characters. Onboarding ended with no dossier, therefore no first read and no ideas. Fix: the allowance is now at least as large as the answer budget (short calls unchanged), plus one retry with a bigger budget when the JSON does not close.
- After the fix the same run returned ok. Real numbers: 10 posts, multiples up to 47.8×, two transcripts, two videos watched by Gemini, dossier v2 written, first read composed referencing her actual rain run with the Taylor Swift audio. **Whole onboarding cost $0.35.**
- **Fourth and fifth bugs, both from the first live conversation.**
  - The first read was deduped on `first_read:<creator>:v<dossierVersion>`, so the live creator got TWO introductions, one per dossier write. In production the dossier is rewritten weekly, so every Sunday would have opened with "took a look through your posts" as if they had just signed up. Now keyed once per creator, forever.
  - **The reply path was the only ungated outbound surface.** The scout, the review and the first read are all judged by the critic; the surface the creator actually talks to was not, and the first live conversation came back with a markdown heading and a bulleted shot list, which Telegram renders as literal asterisks. Replies now go through the critic, which rewrites once and **never blocks** (a person is waiting; a swallowed reply is worse than an ugly one). The critic and the soul both name markdown explicitly. Verified live: the next reply came back clean and still specific.
- **Sixth bug: zero-caller machinery, the dominant defect class again.** `core/messages.expireStaleQuestions` was written, documented and tested, and had **no callers**. So nothing ever closed an unanswered question and the open-question rail muted the scout permanently. My earlier 24-hour rail patch was itself a duplicate of this dead function's job, and contradicted its rule. Consolidated: the per-creator logic is now a shared helper, there is a nightly fleet sweep (`expireStaleQuestionsAll`) wired into crons, the rail is back to the single rule (an open question blocks), and the fixed-hour threshold is gone. Expiry is by the creator's DAY in their timezone, as the original design says.
- Also: `core/messages.recordInbound` was a second inbound door that did not close the open question, while the Telegram door did. Both doors now behave the same. New tests cover answering, day-rollover in two timezones, and that the sweep is actually scheduled.
- **Seventh bug: onboarding starved day one.** Onboarding watches up to 40 of their own posts (`WATCH_CAP`); the daily operating cap is 8 watches. Counted together, every new creator hit the budget rail on the day they signed up and got no idea at all — the worst possible day to be silent. The one-time catalogue watch is now counted apart (`budgets.onboardingWatches`, kind `onboarding_watch`); the dollar cap still governs, so an expensive onboarding still stops the day and a merely thorough one does not.

### ⭐ Exit criterion met live (§8): a real breakout became a real idea
Fixtures off, real vendor, real accounts. The scout found a genuine 10.15× breakout on a tracked account, ran four multi-step lookups of its own choosing — transcript of a rival breakout, the creator's own rhyming posts, the rival account's normal, and the comments on the winner — read from the comments that the interesting thing was miles-versus-kilometres, connected it to the creator having just moved to Brisbane (from her dossier), and wrote a shootable idea with a hook, on-screen text, length and sound, citing the real link and the real multiple.

### The voice pass (§13.10): quote them, do not describe them
- **The root cause of the generic caption.** The writer had never seen a sentence the creator wrote. Her voice reached it only as the dossier's adjectives ("punchy, self-deprecating text hooks"), so it filled the gap from training data: "waking up at 5am and convincing myself it was pure marathon discipline". Grammatically hers, actually nobody's.
- `convex/agent/voice.ts`: her best lines verbatim, ranked by multiple, drawn from captions and from the on-screen text and spoken hooks the watch pass already reads off her own videos, capped at 8. Plus counted habits no model can hallucinate: median words, lowercase-start rate, emoji rate, question rate, hashtag count. Wired into every one of the eight `buildPrefix` call sites, with a coherence test so a new skill cannot silently lose it.
- Rules that ride with the evidence: the overlay IS the joke and never explains it; one concrete noun from their life and no abstract nouns; if any creator in the niche could post the line word for word it is wrong; no borrowed formats ("pov:", "it's giving", …).
- Critic gains `generic_line` and `vague_sound`; the judge gains a `soundsLikeThem` dimension, defaulting to 3 so it never punishes a message that proposed no line.
- **Verified live on the same creator.** Her real lines are things like "i run so i can rot the rest of the day 😚" (47.8×) and "i can't wait to shower, put my pj's on and go to bed (it's 7:00 am)". Asked the same question as before, the new overlay was "running 6 miles at 6am like i have anywhere to be in brisbane today" — lowercase, no abstract noun, joke not explained, Brisbane from her actual life, and recognisably the same joke as her 47× post.
- **And the pass found its own bug.** She named a specific Taylor Swift track having made no sound lookup that turn: a real song, on-brand, and unevidenced. Demanding a named sound had pushed her to remember one instead of looking one up. The critic now receives `toolsUsedThisTurn` and has an `invented_sound` tell, because a critic that cannot see what was looked up cannot tell a checked fact from a remembered one.
- Also confirmed working: the critic's primary timed out on that turn, the DeepSeek fallback caught it, and the reply was rewritten before sending. 340 tests.
- **Critic latency, tuned the hard way.** The critic's primary (`z-ai/glm-5.3-flash`) timed out on **five consecutive live replies**, always at the shared 45s default, so every reply paid 45 seconds of silence before the fallback started. Added a per-call timeout. I first set it to 12s, and then BOTH models missed it and a live reply went out ungated carrying the exact vague sound the critic exists to reject, so a gate that never runs is worse than a slow one. Settled at 25s, which is what the DeepSeek fallback was measured completing in. Also corrected the failure log, which blamed `max_tokens` for what is a latency fault and sent me looking in the wrong place.
- ⚠️ **Operator decision:** the critic's primary has never once answered in time on this deployment. `MODEL_CRITIC` is an env var, so swapping it to a faster model is config, not code. Worth measuring before the pilot; right now every reply pays 25 seconds of dead time before a fallback that works.
- **Final live check, all fixes in:** asked for a rest-day idea, she returned "calling a walk to get groceries active recovery so i don't feel guilty" with sound "your own audio". Her register, no explained joke, no abstract noun, and an honest sound. 342 tests.

### The real-world pilot began (2026-09-02 evening)
Fixtures OFF, live vendor, all 19 crons running on `impressive-roadrunner-997`, operator's real Telegram paired to one creator (@vanessaalopezz's public catalogue).
- **Bug: two creators shared one Telegram chat.** Inbound routing resolves a chat with `.first()`, so replies landed on whichever row the index returned and both records sent proactive messages to the same phone. Pairing is now exclusive on both the real and the dev path: re-pairing moves a chat and releases the previous creator.
- **Bug: `status: "gone"` was in the schema from the start and nothing ever set it.** Two deleted handles 404'd on every six-hour sweep, burning a request each time and filling operator alerts with what looked like a vendor fault. The sampler now retires a deactivated handle, says so once, and never samples it again.
- **First real send.** Live sampler across 6 accounts produced 11 signals; the scout picked a genuine 10.8× breakout, ran three lookups, and delivered to the operator's phone an idea grounded in her own numbers ("your arrival post did 1.2k, but your short selfie run clips regularly hit 10-25× normal") with an overlay in her register and an honest "your own audio". 346 tests.

### Eval scenario set frozen (found by the tooling research)
`eval.run.scenarioCreators` returned "every paired creator with a dossier", so the eval's dataset changed every time anyone signed up or paired. A baseline whose membership moves between runs measures nothing, which made the regression gate unbuildable regardless of tooling. The set is now declared by handle in `SCENARIO_HANDLES`, a missing scenario is loud, and the run reports `comparable: false` rather than quietly returning a smaller suite. Also noted: `eval.run.scout` still has no cron and no CI caller, so it only runs by hand.

### Two bugs the running pilot surfaced
- **She apologised for an outage that never happened.** The creator-facing "i couldn't see tiktok today" took the newest ScrapeCreators health row of ANY kind, so a nightly COST RECONCILIATION mismatch ("vendor 50 vs ledger 114") reached a live creator as an outage. An accounting discrepancy is an operator problem and has nothing to do with whether she can see the platform. Only read-health checks can trigger it now.
- **A time-bomb test.** `consolidate.test.ts` seeded a note's expiry from the real `Date.now()` while running the job at a fixed simulated date, so it silently stopped testing anything the moment real time passed 2026-09-03 03:00 UTC, which happened mid-session. Both now use one clock. ⚠️ Five other test files mix a real clock with fixed dates (`jobs`, `recordedShapes`, `review`, `simulatedDay`, `taste`); they pass today and were not audited.
- Pilot health at this point: fixtures off, crons running, credits 930 → 884, own posts 10 → 16, two messages delivered to the operator's phone, spend $0.57 for the day.

### The regression gate (§18 gate 9) — written here, not bought
`convex/eval/gate.ts`. Runs the scout suite over the declared scenarios, aggregates checks and judge scores, and compares to a frozen baseline in `evalBaselines`. Tolerances are deliberately loose so it does not cry wolf; a gate that is ignored is worse than none. `sentRate` is measured alongside `passRate`, because a change that silences her would otherwise score a perfect pass rate.

**Decision: write it, don't buy it.** The research recommended Promptfoo, and its own strongest finding was that we had a coding gap, not a tooling gap. Everything the gate needs already runs inside Convex next to the data; a third-party runner would need us to expose an HTTP surface to drive what we can already drive internally, plus a new dependency and creator content leaving our infrastructure. The golden cases, which are the real work, are ours either way and portable if we ever change our mind.

**Three bugs the gate found before it produced a single number.**
- The scout suite ran with the delivery rails engaged, so a suite run at 1am for the creator's clock measured nothing at all. Measurement now bypasses environmental rails, guarded so it is refused unless `dryRun` is also set, and tested.
- A scout run writes a verdict on every candidate it judges, so a second run saw an empty board and "went quiet" — a regression that was really the first run eating the evidence. The gate resets the board first.
- **A creator's timezone defaulted to `America/Los_Angeles`.** The worst kind of default: plausible enough that nobody checks it, and every quiet hour and send time then wrong by hours. Found because the operator in New York had a Pacific creator record and the scout refused all morning. Now UTC, which is obviously a default and gets corrected.

**First baseline:** pass rate 0.5–0.67 across runs, sent rate 1.0, judge clean (corny 0, generic 0, specific 3, wouldSend 3, soundsLikeThem 3). The failing check is real and actionable: `has_link`, "link only in evidence" — a scout message about someone else's post that never included the link.

### Chasing the gate's first finding: five more bugs
The gate's `has_link` failure was real, and pulling on it found a chain.
1. **The check itself was wrong twice.** A link present only in the evidence PASSED, though nobody can click evidence; and when there was no link anywhere the failure detail read "link only in evidence", a lie that cost real debugging time. A scout message must carry its link, full stop.
2. **Recording a verdict destroyed the link.** The URL was parsed back out of the `why` sentence, and `setVerdicts` overwrites `why` with the model's reason. So a re-judged signal lost its link permanently and she wrote about a post the creator could not open. Signals now carry a durable `url` set at detection, on all six writers, with a test that no writer can omit it.
3. **The eval measured something production never sends.** The scout's dry run returned BEFORE the critic, so the suite scored raw writer output. The critic's `no_link` rule catches a missing link every time; the defect was only visible because the measurement skipped the gate the real path goes through. Dry runs are critiqued now, and the pass rate went from 0.25 to 1.0 — the fix and the measurement arriving together.
4. **The gate reported a regression that was its own ruler moving.** Tightening a check made the pass rate "drop" 25 points against a baseline measured the old way. Baselines now record a `RUBRIC_VERSION`, the gate refuses to compare across versions, and `rubric.test.ts` hashes the checks and the judge prompt so the ruler cannot change silently.
5. **A missing judge looked like a quality collapse.** Unjudged runs yield zeros, and a zero reads as both "not corny" (good) and "not specific at all" (bad). Judge dimensions are only compared when both sides were judged, and the gate refuses to freeze a baseline nothing judged.

**And a second time bomb, this one a real production bug.** `plan.applyEvent` stamped `pastDueSince` from wall-clock time instead of Stripe's event time, so a webhook Stripe retried for two days started the grace period two days late and a delinquent creator silently got five days of proactive work instead of three. Found because it made a billing test fail the moment real UTC time crossed a fixed date. 373 tests.

⚠️ Known limit: recording a baseline at n≥2 exceeds the action budget now that dry runs are critiqued. n:1 works. Split the suite from the summary if we need more samples.

### Why the pilot went quiet: four causes, one of them serious
1. **My fault:** I left `SCRAPE_FIXTURES=recorded` on after the gate run, so she was reading replayed fixtures, not the live world. Off again.
2. **An unanswered question blocked her for a whole day.** The nightly expiry runs at a fixed UTC hour, which is 10pm Eastern, so a question asked Tuesday evening blocked all of Wednesday. Now hourly, so it lands just after each creator's own midnight wherever they are.
3. **She said the same thing twice.** The retirement message was keyed by tracked-account row, and duplicate rows exist for one handle, so the operator got "@runwithcarly isn't up anymore" twice. Keyed by handle now.
4. **⭐ The lane sweep had NEVER worked in production.** Every keyword read failed with "Value is too large (2.19 MiB > maximum 1 MiB)" because the entire vendor payload was being cached. It passed on fixtures only because those are trimmed to six posts. Downstream code reads exactly three fields of a post's raw payload (`is_ad`, `music.id`, `author.unique_id`); everything else is bitrate ladders and avatars. The cache now stores what readers use, with a size guard that trims rather than losing the read. **The "shape" signal source came online for the first time: 6 keywords, 0 failures, 12 signals.**

Follow-on: lane signals come from accounts nobody tracks, so there is no per-account normal and their score is not a multiple. The writer cited one and the critic caught it as `invented_number`. The skill now forbids a multiple for a shape candidate and the evidence carries a null ratio rather than a number the model will read as one. 379 tests.

### The roster now grows from the lane (§13.9)
**The gap.** The watchlist was set once at onboarding and never grew. Onboarding asks for three handles and offers suggestions from two sources, and BOTH are unreliable: `discover.creators` hits TikTok's Creative Center, which is down at the vendor, and `account.following` needs the creator's following list to be public. A creator who cannot name three accounts from memory watched almost nobody, forever — the single biggest cause of her having nothing to say. The live pilot creator was down to **two** active accounts.

**The fix, from evidence we already collect.** The lane sweep records every top-of-lane post for their keywords, with its author. `convex/scout/roster.ts` counts those authors and, when one recurs with real numbers, offers it once with a button: "@x keeps coming up in your lane — 3 posts on 3 different days, median 70,952 views. want me to watch them?" Their yes goes through the same `addTracked` the web control and the chat use. Conservative by design: one offer at a time, never while another question is open, never someone they already removed, and it stops growing on its own at 15.

**A bug it had within a minute of running live.** The first real candidate was a single 1.28M-view post that had been sampled on two consecutive sweeps, which my "one viral post is luck" filter counted as two days. Recurrence now means the day they POSTED and two distinct posts, so no amount of sampling can fake it. The next run found a genuine recurring creator instead. 387 tests.

### "I tapped the button and nothing happened"
The work HAD run — twice. `@andi.renay` was added to the roster and both confirmations were delivered. What was missing was any sign that the tap registered: `answerCallbackQuery` was called with no text, and the inline keyboard stayed on the message, so the only feedback was a new message arriving seconds later after the whole converse path had run. The operator tapped, saw nothing, tapped again, then typed "yes" — three inputs for one decision, and two identical replies.

Fixed at the webhook, before the work is scheduled: the tap gets a toast ("on it" / "ok, noted") and the buttons are removed from the message immediately, so it cannot be tapped twice. Three tests assert the acknowledgement happens BEFORE the job is scheduled. 390 tests.

⚠️ Worth noting for the roster: the offer message carries `awaitingAnswer: true`, and the roster reply path does not close it explicitly — it closes because any inbound message closes the open question. That is fine, but it means a tap costs the day's one open-question slot until answered.

### Roster ranking and the question slot (2026-09-04)
- **Ranking flaw, found on the real lane.** Sorting by consistency alone put a 6,474-view account above a 955,927-view one, because the small one posted on four days and the big one on two. An account doing a fraction of your numbers teaches you nothing however reliably it posts. Candidates now need a median of at least half the creator's own normal, and rank on days × log(views). The 955k account now tops the list.
- **The offer was holding the one open-question slot.** "At most one open question" exists so a creator never carries two pending decisions, and an optional button offer is not a pending decision — if they never tap it, nothing is lost and the dedupe key means it is asked once. It was blocking the next offer behind an unanswered idea, which inverts the priority: the idea matters, the offer does not.
- **`offer` reported sends that never happened.** The dedupe key writes nothing on a repeat, but the action still returned the handle as offered, making the fleet counter a lie. It now trusts the send result. 391 tests.

### Sprints 4b, 4c and 4d built (2026-09-04)
**4b — she runs your week.** Post-time model from their own posts (best hours, weighted, "thin/solid/none" so a default is said as a default). A pure planner: gap finder, editing block sized from their cut rate and **default on**, cadence clamped, days from the dossier. The Sunday draft writes film/edit/post blocks and one message with a book button; one tap consents every block and schedules reminders. Three touches per booked block, re-validated when they fire so a moved or dropped block produces nothing: prep with the shot list, a check-in fifteen minutes before with yes / push it / skip, and a post nudge that **only fires when she knows they filmed**. Reminders are exempt from the daily cap and refuse quiet hours; the check-in supersedes an open question. "Push it" proposes a real gap today plus tomorrow. **The plan is in every prefix with block ids**, and five belt tools read, move, drop, add and replan it by text. A calendar file carries the week when nothing is connected, which is why Apple CalDAV stays cut.
**Verified live:** the plan landed on the operator's phone; "whats on this week? and move the saturday film to sunday 4pm" moved the film AND its edit, confirmed against the rows.

**4c — she grows with you.** Outcomes write back: a posted idea's multiple, two days on, folds into taste with **no tap from the creator** — a win raises its features, a flop lowers them, scaled by how far it went and capped so one freak post cannot rewrite taste, and **a result outranks the tap before it**. Nightly sweep, idempotent. Voice exemplars already rank by outcome. Standing ("you have read 18 of their posts, which is a thin read: hedge") bounds how bold her claims may be and gets bolder only as the evidence does. Patterns need three wins on the same feature. Milestones ride an existing win message rather than taking a touch, said once, recorded in a row. **The prefix stays a fixed handful of lines at simulated month six**, asserted.

**4d — finding the lane.** She reads the lane from their hashtags and captions weighted by what performed, drops the noise everyone uses, and states it back on the first read for one tap; "not quite" offers the two nearest neighbours, never a blank field. The tap confirms the keywords she actually proposed, kept on a row. Confirming repoints the dossier keywords, which is what the sweep and roster read, so it is a real change. Drift: the weekly rewrite compares keywords and asks once per rewrite when most of the lane has moved.

**The deletion-coverage guard earned its keep again**, catching `laneReads` before it could become a table that survives an account purge. 447 tests.

### Sprint 4e built and live (2026-09-05) — connected numbers
Two throwaway accounts connected on Zernio (one TikTok, one Instagram). Their real responses were recorded FIRST (`integrations/zernio/fixtures.recorded.json`), and every line below was built against that recording, not the spec.
- **The zero.** The recorded TikTok post: `views: 107, impressions: 0, reach: 0, igReelsAvgWatchTime: 0`. Zernio's 0 means "not applicable / not synced". `connections/analytics.ts` maps platform-inapplicable metrics to null by construction, and a 0 on reach/impressions while views are positive to null (a sync gap, not an audience of nobody). Named tests on the real rows.
- **A live bug in the existing connection path.** `listAccounts` sent `limit` without `page`; the API answers 400 ("page and limit must be provided together"), so every reconcile had been coming back "unreadable". The spec does not say so; the live API did.
- **Ingestion is code:** bootstrap pages ninety days per account (page AND limit), joins to own-posts by native id then platform URL (scraped Instagram ids are the numeric pk, Zernio hands us the shortcode), refreshes public counters only when the connected read is newer, writes `reachMultiple` once three posts carry reach. The delta feed runs hourly on a stored cursor and on the `analytics.synced` webhook (re-registered); followers daily into `followerSnapshots`. Failures are `vendorHealth` rows. **Live:** bootstrap wrote 4 rows, the cursor is stored, 2 follower snapshots.
- **One rule for citing their numbers** (`connections/numbers.ts`): connected wins and is labelled with its age; stale (>48h) falls back to the public count and says so; what a platform hides is said once, never guessed. Two belt tools (`own_post_numbers`, `post_diagnosis`) and the four diagnoses worded once. A `mixed_basis` check and critic tell: a TikTok number and a retention figure in one claim fail unless the message names the same video cross-posted. Rubric 3.
- **Consumers re-based on reach where it exists, per the audit:** the win, the rung's multiple (with its basis), the review evidence (reach, retention, skip rate per post), the opinion on their own post (labelled numbers + what the platform hides), the post-time model, outcomes, voice ranking, and the personal prefix.
- **Gate:** it recorded an empty baseline once after a broken deploy; it now refuses to freeze a run in which she sent nothing, and the bad baseline was dropped. 475 tests.

## 2026-09-05, late — the pilot moves onto the operator's own connected accounts

Sprint 4e done; rubric-3 baseline recorded (needed a retry: the scout sent nothing on the first sample, then sent; n=1 is thin and the action budget blocks n=2). Then the pilot creator was re-seeded on the two accounts the operator actually connected (tiktok kevin.castro9996, instagram heymaya182), so scraping and Zernio point at the same content. Reading those accounts live found five bugs in forty minutes that 477 green tests and a recorded fixture never showed:

1. Instagram returned `pk` as a number on this account; the schema demanded a string, so the whole catalogue read failed. Ids now accept either and become strings.
2. A TikTok photo carousel came back from the video endpoint with a `/photo/` share url; the normaliser called it a video and the transcript endpoint refused it. Photos are photos.
3. One post, two rows: the connected feed created the row keyed by the id in the URL, the scrape keyed Instagram by the numeric pk, and neither joined the other. The scrape now claims a row with the same URL and keeps its connected numbers; a dev helper merged the twins.
4. `contentType: c.durationSec ? "video" : "video"`: a row the connected feed created was always a video. An Instagram row without a duration is a photo.
5. A corrected normaliser did nothing for seven days: the read cache served the old shape and the ingest only refreshed metrics on an existing row. Identity-level facts now follow the latest read; a batched `forgetReads` helper exists (a whole kind at once trips the 16 MB read limit).

Result: 10 own posts, 4 carrying connected numbers, photos typed as photos, no errors on a fresh read. The belt answers "how did the reel do" with reach 3 connected, read 0h ago, and says what Instagram will not tell her.

Lesson, again: a recording of one account is one account. The second real account broke the shapes.

## 2026-09-06 — the backfill had no caller

Mapping the onboarding flow for the operator found that `connections/sync.bootstrap` (ninety days of connected history) was never called from product code: connecting from Settings registered the account and started the hourly delta, and the past stayed empty. Zero-caller machinery, the dominant defect class in this product's history, one more time. It is now scheduled where "connected" is decided (`applyAccounts`), once per newly reporting account, with a test. Also recorded: the "three posts you wish you'd made" opener for a creator with no posts is not built; connecting accounts is paid-only, so no creator has connected numbers during onboarding by design.

## 2026-09-06 — Sprint 4f: the scattered account, and the growth plan

The operator asked how she guides someone whose posts go in every direction and who does not know how growing works. Designed into the plan first, then built: `onboarding/clusters.ts` groups their posts by the embeddings we already run (cosine, one threshold, a cheap naming call with a words-only fallback) into a `lanes` object on the creator with share and median multiple per group; `proposeLane` triangulates what their audience rewards, who they admire (the roster's observed keywords), and what they said, and surfaces the rewarded-vs-admired split as the one question; the first read carries the proposal as its single question with candidate buttons (`lanepick:`), capped at two lane questions a week from the rows; a tap writes the lane and starts a four-week `growthPlan`; `agent/growth.ts` holds the playbook (platform-stated vs trade-reported vs folklore she must not state as a number) and rides the prefix only while she is new, the lane is unconfirmed, or a plan runs; the week plan takes the plan's cadence; the Sunday review gets in-lane vs out, medians per side, follows and the follower delta, and must say whether the plan is working. 495 tests.

**Live, after 4f (2026-09-06 midday).** Running the cluster read on the operator's own scattered account found: a group of test posts at 0× offered as "rewarded" (rewarded now means above their normal); the admired roster could not become a candidate without a matching cluster (it is one now, with "no posts there yet" as its evidence, so the split question fires on day one: "your best posts are the travel ones, but the accounts you admire make running. which one do you want to be?"); the roster's words did not exist until the sweep sampled them (read at onboarding now, cached); the Gemini embedder returned **429 Resource exhausted** on sequential single calls AND on the batch endpoint, so the read changed shape between runs. Vectors are now stored on the post row and each post is embedded once; labels are reused when a group is mostly the same posts. ⚠️ Operator: the Gemini project's embedding quota is the free tier; enable billing on that key before a second creator onboards or the cluster read degrades to singletons.

**The fresh run (2026-09-06 11:56).** Both pilots deleted through the real deletion path (it also deleted the Zernio profile, correctly; connected numbers need a reconnect). Onboarded through the same functions the form calls (`onboarding/start:onboardAsUser`), paired by token. Three minutes to the first read, $0.19, no failed jobs. Her message named two real posts and read the style right, said the data was thin, and carried the two candidate buttons — but rewrote the proposal's question into her own ("grow around the software builds, or keep this a casual log?"), so a tap on "runner track" would answer a question she never asked; and it cited "40 percent" where the cluster read said 30. Rail added: when a message carries candidate buttons, the text must name every candidate; one rewrite, then the proposal line is appended.

**Afternoon, 2026-09-06.** Two more live defects on the fresh pilot: the Sunday review fired on a three-hour-old account ("zero posts this week, we just started"), and its critic rewrite handed the JSON envelope back, so raw JSON reached the operator's phone. Fixes, enforced in code: no review before five days together; `core/messages.send` unwraps a `{"message"}` envelope and refuses one without a message (a person never receives JSON, whatever skill produced it). Designed and built first contact: pairing before the first read now sends a hello ("hey, i'm maya. i'm going through your posts…"), and the first-read skill is written as first contact, who she is and what she does, then the read, then the one question. Pilot re-onboarded to see it live.

**First contact, live (14:48).** Three more defects on the way to it: the hello sat queued four minutes because the minute drain ran the ingest inline and Convex skips a cron tick while the previous one runs (long jobs now run in their own action; pairing drains deliveries at once); the critic dropped first contact twice for "remove the compliment" and once for length (first contact is now advised, never blocked, and gets the character cap that fits an introduction plus a read). Delivered: "hey kevin, i'm maya, your assistant for tiktok and instagram…", two real posts, the split question naming both candidates, buttons matching the question, 100 words, 7 minutes after pairing including three retries.

**Day one is a working day (15:31).** The operator caught her telling a Sunday signup "on sundays i lay out your week": the plan was a Sunday-only cron, so a Tuesday signup would have heard nothing about their week for five days. Now the first plan follows first contact by twenty minutes, covers the rest of this week (slots scaled to the days left) or next week when fewer than two days remain, and the Sunday cron then stays quiet. Running it live refused with "nothing to plan with": day one has no ideas and no experiment. The first plan is now seeded by the model from their own posts, the proposed lane and the growth plan, stored as idea rows. Delivered to the pilot: "next week, 1 post: mon 5 pm film, edit 6 pm, post 8 pm — Locals walking past a view people save up all year to see", the hook from their own best post. One slot because the dossier's cadence on ten posts is one a week; the growth plan's three a week takes over once a lane is tapped. Also today: she said hello twice (fixed), and one message row can now go out as several short texts.

**The calendar is an assistant's, not a scheduler's (15:40).** The operator asked whether she makes sure an idea they like ends up on the calendar. She did not: calendar moves happened only when asked; a save tap answered "it's in your swipe file"; a warm reply was taste and nothing else. Built `calendar/secure.ts`: the next free filming slot as one tap, on a save tap and after a warm reply; the standing rule in the conversation skill. Found on the way: the web/dev inbound door wrote no `kind`, so a warm reply through it never counted; both doors now write `inbound`.

**Evening, 2026-09-06: the first day and the day-to-day, decided and built.** Day one now reads hello → the read with the lane question → the plan the moment the lane is tapped (or at twenty minutes) → the roster sampled and the scout judging right away, gated on a real signal; four touches at most, all work. The draft invitation moves to day two. Each plan line carries its why. The scout and the review send short texts like the first read does. Live on the pilot: the same-day sample found four breakouts on the roster; the scout judged "nothing worth their time today" (a running roster against a travel lane, which is the right call). Also: the operator-alert cron shares the operator's own chat with the pilot, so "🩺 smoke failed: scrapecreators/reconcile" looked like Maya talking; that alert is real (vendor 194 credits vs ledger 60 today) and is the next thing to run down. ⚠️ Operator: a separate Telegram chat for `TELEGRAM_OPERATOR_CHAT_ID` before any real pilot.

**Night, 2026-09-06: the conversation gauntlet, read as a person.** Fourteen probes through the real path on the new scenario rows: 14 replied, 11 passed the checks and the judge. Substance was strong (growth grounded in her own 300–600× posts; watch time honestly refused; the hostile probe answered with specifics and no grovelling; a commitment got "monday 8am work to film on your run?"). Three voice defects: nearly every reply ended on "want me to…?" and a bare "hey" got "what's on your mind"; plumbing words ("baseline", and a code string, "from the next pass"); and an invented price ("early access, completely free"). Fixes: the soul says most replies end on a statement and forbids the plumbing nouns; the prefix carries the plan status, trial date and the one price line (`PLAN_LINE`); deletion is Settings + DELETE. Re-probed: price verbatim, deletion right, "i'm software. just here to keep an eye on your numbers", growth hedged with "my read is early". The reflexive question is only partly gone; the judge keeps scoring it. Also: `mixed_basis` exempted an honest refusal (rubric 4); the scout gate against the new scenario rows: pass 1.0, sent 1.0, judge 3/3 on every dimension, no regression.

**Late, 2026-09-06: fun and pleasant.** The operator, reading the gauntlet: she is competent the way a strategist is, not the way a friend who watched the video is. Soul: she watched it as a person before she read it as a strategist; react first, the moment named from the evidence (never a timestamp she was not given, which the first try invited and the critic caught as invented_number); permission to be fun. The critic no longer calls a named reaction slop and carries a no_reaction tell; rubric 5 adds `reaction_first`, measured. After: "hey" → "hey. we still have thursday at 6pm down for that running clip." The scout now opens on what lands ("the lack of preaching: just the target pace, the actual split, and the blunt realization at rep four") and ties the idea to the booked block. The opinion still opens on numbers two runs in three; the tell is the lever.

## 2026-09-07 — her calendar sense

Overnight the pilot got its first real day: the 8:06 scout (an Ali Abdaal tier list, viewer-first, tied to his lane), the shot list on a tap, and "book it for my next open slot" on a Monday morning answered with Thursday, then "why those times" answered with a guess about the evening scroll window. She could see blocks and events, never free time; "today" did not exist; and her posting-hour reasons were not hers. Built `calendar/availability.ts`: free filming windows from now (today counts before 7pm), each with why (their usual hour, the next free hour, or "the default until they name theirs"), best posting hours from their own posts with the sample size, in every prefix and as a `calendar_free` belt tool; the rule that a question about when gets the first window as an answer, never a question back. Found on the way: `preferredSendHour` (the hour they reply in) was being used as their filming hour in three places, so "your usual filming hour" was 9pm; and an edit-shaped question ("scrap that, what's my next open slot?") took the idea-edit route and got a canned line when there was nothing to edit. After: "today at 5:30 pm, which is the default filming slot until you name your preferred hours." → "booked for today at 5:30 pm."

**Afternoon, 2026-09-07: the six.** (1) Urgency: every scout idea carries "now" (breakout, sound, shape, calendar) or "any"; the slot offer takes the first window for "now", a habitual day for "any", and says why ("this one goes stale in a few days, so today 5 pm…"). (2) Habits: after two booked or moved film blocks, the hour and days are theirs (`calendar/habits.ts`), read by the free windows, the week plan and the offer; before that the default says it is one. (3) The calendar as a source: signals already fire two days ahead; calendar ideas are "now". (4) Edge-case scripts 1 and 2: a Google event moved under her moves the block, keeps its length, re-schedules the check-in and she says so once; a removed event drops the block and she says so without a question (`sync.upsertEvents` → `followMoves`; new index `by_creator_event`). (5) The opinion's opening is measured by `reaction_first`; three more live runs before any structural change. (6) Operator: an ops Telegram chat, and Google connected on the pilot, without which 3 and 4 run only on the fake model. 530 tests.

**2026-09-07, afternoon: she watches them the way a person would.** The operator asked whether the watching yields what a human picks up: how they make things, their personality, how they shoot, what they look like, their humour. It did not. The card was a shot log (hook, beats, framing, lighting, pacing, text style, sound type, signature): craft-rich, person-poor; and nothing after onboarding was ever watched again, so her picture froze at day one. Built: a `them` block on every card (look as style, voice as manner, humour and at whose expense, presence, their world, what they care about) plus "the one thing a friend would notice", with the guesses a friend would not make forbidden (age, ethnicity, body, health); the dossier persona carries the same six fields from the cards only; every new post is watched after onboarding (`watch.watchPost` from the readback). Found on the way: a re-watch got 403 on the cached signed media URL, and `force: true` on a read was ignored at the cache claim, so the "fresh" read was the stale one. Fixed both. First real card on the operator's Piccadilly pan: "they love taking in the nighttime city energy and filming a quick full-circle panorama of where they are."

**2026-09-07, late afternoon: she remembers their posts.** The operator asked what she pulls at onboarding and how the hyper-personalisation grows to the point of bringing up characters and past posts. The inventory was rich (catalogue, transcripts, watched cards, dossier, roster, clusters) and the growth loop real (taste, outcomes, notes, weekly rewrite, standing, milestones, memory of ideas and notes) but their own posts were not in her searchable memory: "the dog barking at the tv" found nothing for a video captioned "I have no words". Built: every own post indexed into `memories` (kind `post`) as what it is and what she saw in it (`agent/postMemory.ts`), at onboarding and on every new post; `own_rhymes` by meaning first; three callbacks chosen from rows each turn (`agent/callbacks.ts`: a saved idea never filmed, a recent win, a live note, their world) in the prefix, with the soul saying bring one up when it fits. Live: "the dog barking at the tv" → 0.77 on the February clip captioned "I have no words". 537 tests.

**2026-09-07, 14:10: warm first, honest always.** The operator read the fresh opening and asked for warmth: the read had become a review ("raw plumbing", a percentage) three minutes after hello. First contact now opens with the thing of theirs she genuinely liked, explains the arrangement like a friend describing a new setup, keeps a day-one critique to one gentle clause, and asks the one question warmly; the soul carries "warm first, honest always: a person reading you should feel seen, not processed". Re-run on the pilot (dev `redoFirstRead`): "that night pan across piccadilly… is genuinely great. the quiet ambient city audio does all the work." Two more live findings: the hourly scout cron landed an idea in the same minute as the read (the scheduled run had a delay, the cron did not; the gate now holds any idea for thirty minutes after the read), and "critic note" on the read was a critic TIMEOUT, not an objection: GLM timed out on a third of critic calls all week. The critic's primary is DeepSeek now, GLM the fallback, still a different family from the writer.

## 2026-09-08 — show, don't tell (Sprint 4g)

**Afternoon.** The operator asked whether Maya could make her ideas visible with Google's image models and a selfie: "a way for maya to show the user what she's really thinking, not just through words." Design first (§22): a two-to-four frame storyboard, not a video (cost, latency, and a bad video of *them* is a cringe where a bad still is a sketch); not their face in phase one (a consent line: the person is a figure from behind, hands, or out of frame); their setting from the dossier and a frame of their own best post handed to the model as the light to match. Three doors, one path: a "show me" button on every idea, a `show_frames` belt tool for "can you picture that" in words, and the scout marking a pick `visual` when it lives in a look rather than a line. The writer answers one call with a shot list; code clamps it, fans out one image request per frame in parallel (Nano Banana 2 through OpenRouter's image API, the older Flash Image as the fallback), stores the frames on the idea row, and sends one album with her intro. Budgets, never booleans: five sketches a creator a week from `ideas.framesAt`, a cost row per frame, a fleet cap of eight renders in flight (`render_frames` is a long job kind in its own action; a burst defers instead of a 429 storm). The prompt forbids app chrome, numbers, logos and watermarks so a frame can never pass for a screenshot; the intro says it is a sketch, not a post.

**Built:** `integrations/openrouter/images.ts` (the vendor call, never throws, a 1×1 PNG under the fake model), `agent/frames.ts` (skill, plan clamp, prompt rails, weekly count, render, the three doors' enqueue), `sendTelegramMediaGroup`, `messages.frames` → the album after the text, `render_frames` in the scheduler with the in-flight cap, the button in the scout and the moment, the tool on the belt, `frames` as a taste event (1.5, same as a shot-list tap), the soul's one line, two gauntlet probes, DELETE wipes the frames. 14 new tests across the five categories; the simulated day now sees the album follow the fake scout's visual pick.

**Live, on the two scenario creators, real model:** two real dry-run picks seeded as ideas and drawn. Vanessa's 400m repeats: three frames on a sunset track, legs from behind, "lap 4 of 8 (questioning every life choice)" bent over hands-on-knees, then flat on the grass with "lap 8 done. do not speak to me for 6 hours." Brett's "why 90% of travel creators quit in year one": a dark desk with a passport and drives, a suitcase of batteries and receipts, "it's a cash flow test, not a vacation." Her intros: "rough sketch of how the 400m repeat countdown looks on screen, not a finished post." 23 s and 43 s wall clock; $0.20 and $0.34 a storyboard, above the $0.15 estimate because the reference photo rides in as input tokens; still under $1.50 a creator a month at five a week. One rail leaked once: a face in profile in one of six frames. The prompt now says "not even in profile; the camera is behind the person, over their shoulder, or on their hands." Found on the way: `s`-flag regexes fail the root tsconfig target; a chained `; deploy` after a failing check reached dev twice, which is exactly the gate `if [ $EC -eq 0 ]` exists for.

**Evening, 2026-09-08: the real path, end to end.** A bare paired creator (`dev:seedBare`, no catalogue read), one seeded idea, and "show me" asked through the belt tool: the job was queued by `show_frames`, picked up by the minute drain, rendered in its own action, and the row landed on the operator's phone as text plus a three-photo album (`deliveredAt` set, no error, Telegram message 164) inside a minute. Her line: "Rough sketch of the beats so you can picture the timing before you touch a box." Unpaired after. 555 tests, pushed.

**Later, 2026-09-08: them, without their face.** The operator asked whether the frames use what she knows of the creator: their look, their environment. Partly: the writer had the dossier and the skill said "their setting", and one still from their top post rode in as the light. Nothing forced the details into each image, and one still is thin. Now the plan carries a `them` line (clothes as style, hair as style, their usual place with its real details, their props and characters; from the dossier and the cards only, "unknown" otherwise), pinned by code into every frame prompt, and three stills from their top posts go in instead of one. Their face stays out for good: the operator called it too weird. Also answered: no regex decides "can you show me what you mean"; the writer reads the tool description and decides, and the only string match left is picking WHICH idea when she names one.

**Evening, 2026-09-08: the them line, seen.** First redraw of the 5am-alarm idea on Vanessa drew a generic man: her cards predated the them block (watched 2026-09-02), so the dossier had no look and no world, and "unknown" is all the writer could say. Re-watched her sample with the current card (`dev:forgetCards` then `watch.run`, 7 watched, $0.04), rewrote the dossier (`ingest.synthesize`, reason weekly): persona.look "athletic running sets, braided hair, slicked buns", world "outdoor running trails, Brisbane riverfront, hostels". Two failures on the way, both from the three reference stills: first a 12 MB body (three covers at up to 3 MB each) and then "Unable to process input image" from Google, both surfacing only as "Provider returned error" until the client learned to pass the provider's own text through; now stills are capped at 1 MB each and 2 MB together, sniffed by magic number (a CDN's content-type lies), and a frame retries without stills before the fallback model. Redrawn: her braid, her running set, her riverfront at sunrise, from behind in every frame, "it is 5:00 am. no." on the nightstand. $0.20. The mechanism was right the first time; its input was empty. Every creator onboarded before 2026-09-07 has cards without a them block: the weekly rewrite will not fill it until their posts are re-watched.

**Night, 2026-09-08: one image model, and the cap.** The same idea on the original Nano Banana (Gemini 2.5 Flash Image): $0.116 for three frames as OpenRouter billed it, 18 s, the Brisbane skyline and the Story Bridge from her dossier, one garbled on-screen line in three ("negotatae"). The operator: one model, not routing by content, and the text is the creator's to write anyway. Default is now `google/gemini-2.5-flash-image`, the 3.1 model only as the error fallback. The cap comes down from five sketches a week to three: at the cap, frames add about $1.60 a creator a month; at realistic use, under a dollar. COGS with everything (§3.6 at 200, plus frames at the cap) lands near $5.30 without Zernio and $8.00 with, which is about a dollar over the §3.6 target with Zernio and inside it without; the weekly COGS test is what decides, not the model.

## 2026-09-08, night — her number (Sprint 6, §23)

The operator: creators think Telegram is weird; they want her in the Messages app. Researched the channel first (§23 records it): every "iMessage API" is a fleet of Apple accounts behind a REST endpoint; Claw Messenger is a relay on Render in front of Linq's fleet, run by a solo founder's AI agent, $0.005 a message on the agency plan, no uptime guarantee, text-only outbound in the docs; Linq underneath is SOC 2 with an SLA and quote-only pricing; RCS through Twilio is the official route with buttons and carrier-grade scale, four to six weeks of registration. The operator chose Claw for the pilot. Built tonight, on the fake vendor: `integrations/claw/client.ts` (send, register a recipient, the relay's HMAC signature, the vendor's event shapes read defensively, E.164), `core/imessage.ts` (who a phone belongs to; pairing by texting first, by token or by the number they typed; the menu line that stands in for buttons and the pick that reads a number, a yes/no, or the exact label; tapbacks mapped onto the reaction path with emphasize and question noticed and dropped; attachments fetched into the same file path; delivery of one row with the menu on the last text), `imessage/webhook.ts` at `/imessage/webhook`, and `services/claw-relay` (holds the vendor's WebSocket, signs and forwards; a health endpoint the smoke check reads). The one writer now stores every row on the creator's channel whatever the caller said, so thirty call sites that say "telegram" keep working and a creator on a phone never gets a row that lies; `deliverMessage` stays the one delivery function and delegates. Onboarding asks for the number on the last step with one line of consent and registers it on the line at once; the pairing page opens Messages with START prefilled, or shows a QR and the number to type. A phone belongs to one creator. Deletion's final text goes to the phone. Found on the way: a mutation named `useTelegram` reads as a React hook to the linter; the reacted-message lookup found the inbound reaction row first (it carries the same vendor id) and answered with no idea until it preferred her own outbound. 16 tests, 571 total. Not yet live: the vendor account, the key, the line number and the relay's secrets are the operator's; the first real send is the exit criterion, and the REST send shape is documented, not yet observed.

**Later, 2026-09-08: the onboarding is the landing's room.** The operator: the onboarding must match the new landing. Built `app/onboarding/Shell.tsx` and `onboarding.css`: the landing's tokens (cream ground, ink, muted, purple, coral), its two faces (Bricolage for headings, Instrument for everything else, now loaded in the root layout so every page has them), the ink button with the arrow, the eyebrow, chips and rows; the start steps, the pairing page, sign-in and sign-up all wear it, and Clerk's card takes the same variables. Found on the way: a protected route bounced to Clerk's hosted dark portal instead of our sign-in page, because the provider never named ours; `signInUrl` and `signUpUrl` on the provider fix it. The real screens sit behind a session, so `/onboarding-preview` (dev only, 404 in production) shows the steps without one; computed styles verified in the browser on a phone viewport.

## 2026-09-08, late — the human cadence (Sprint 7, §24), and the image skill off her belt

The operator, on the table of what a human assistant would send: "we could build all this in v1." Built: `agent/cadence.ts` with one hourly cron. The morning line at 8 on their clock, only when code finds something true (a shoot today with its time and hook, something of theirs on the calendar, a milestone, three weeks on their own plan said once, or yesterday's block unfilmed, which carries put-it-back / let-it-go buttons); "how'd the shoot go?" ninety minutes to six hours after a film block ends, with filmed / didn't-happen buttons, where didn't-happen marks the block missed and offers tomorrow; a viewer's line when they post, from the card the readback just made, no digits allowed (a rewrite if the writer used one); "saw this, thought of you" from the scout's JSON on a day it has no idea, twice a week at most, no ask; "you alright? no pressure" after a week of silence with things sent, once a month; followers crossing 1k, 10k, 100k, a million, once. Every one goes through the scout's rails (the cap, quiet hours, the open question, the budget, the plan) via a new `railsOnly` query, with one dedupe key each. Found on the way: a query that calls `runQuery` without an explicit return type collapses the generated api's types to `any` for every page (the dashboard pages lit up with implicit-any errors); and the block-button patterns only allowed lowercase ids, which convex-test ids are not. The image skill is off her belt: no tool, no "show me" button, no proactive drawing, no line in the soul, `framesPerWeek: 0`; the code and its tests stay for the day it comes back, with two door tests skipped and a test that a stray tap draws nothing. 13 new tests, 583 total.

**Live, 2026-09-08 20:37 PT.** The revived test creator on the operator's Telegram, three seeded blocks (one tonight, one yesterday unfilmed, one that ended two hours ago). The morning line went out and was delivered: "got you down to film the do i regret it cut tonight at 8:37 pm. yesterday's 4:37 pm box-by-box tour didn't happen though—want me to put it back on the schedule?" with put-it-back / let-it-go. The evening question was then refused with "a question is still open": the one-open-question rail holding across two different touches, exactly as designed. It fires from the hourly cron once the operator answers.

## 2026-09-08, late night — the memory gauntlet (real model, real reads)

The operator: is her memory strong enough for all of this, and can I push a real Maya to its boundaries? Built `eval/memoryGauntlet.ts`: fourteen steps through the doors a phone uses, on the Vanessa scenario creator, every reply through the checks and the judge (suite "memory"), the transcript kept in a row because a long one is not a function result, runnable in batches under the action time limit. **Memory held.** "i'm moving away from running, all in on solo travel" was routed to her own-words field and the dossier keywords already carried it; a turn later she recalled it word for word; asked for her lane she said travel with running as texture; the weekly dossier rewrite did not ask the drift question. Three seeded calendar events became two calendar signals and one private title she never says; asked what's coming up she named the hostel-tour block, the trip and the marathon and not the dentist. "let's film the hostel tour thursday at 5pm" became a booked block. The morning line named the shoot and a milestone; the evening question fired, "didn't happen" marked the block missed and offered tomorrow. **Four real defects found and fixed:** a confirmation leaked "from the next pass" and cut their quote mid-word; the morning line ignored a creator's own "no messages before 10am" (it now fires at the first non-quiet hour at or after 8); the weekly review was dropped for length and, on a rewrite that came back as a fenced JSON envelope, replaced by "something went wrong" (the envelope unwrapper now reads fences, the review unwraps its rewrites and never drops for length); and the eval recorder refused the advisory `reaction_first` check, which would have crashed every scout eval. **Rails seen holding live:** the daily cap, quiet hours, the one open question across two different touches, the budget. Not observed live: the scout on a calendar signal (the cap was spent by the run itself) and the quiet line (an open question, then the cap); both are unit-tested. Both critic models timed out on the review tonight; it shipped marked unjudged.

## 2026-09-09 — the operator's memory architecture, reviewed, committed, fixed, and put through the gauntlet

The operator built the personal-memory layer (docs/PERSONAL_MEMORY_ARCHITECTURE.md): evidence-linked `personalRecords` with exact quotes, reasons and source ids, commitments that read their state from the calendar block; taste split from performance by replaying the event ledger; full-text search over the retained archive with surrounding context, joined to recall; text stored before the embedding with retries; forgetting that invalidates the note, its sources, her reply, the derived records, the rule and the taste events, with an epoch that refuses stale background rewrites. Reviewed and committed as it stood: 595 tests, typecheck and guards green. One change before anyone real is on it: forgetting or correcting one note blanked the whole dossier, the taste note and the growth plan, which put the creator into "no dossier yet" mode for up to a week and dropped a plan that is theirs; now the epoch still fences stale writes, the prose is rebuilt at once from what remains, and the growth plan is not memory to forget. Then the gauntlet, real model: the direction change kept and recalled; a correction in words ("scrap that, running stays the main thing, travel is summer texture") superseded the old direction while the dossier survived; the lane she states followed the correction; the dossier rewrite re-ordered the keywords running-first and asked no drift question. Left for later, as the doc itself says: forgetting scans every message in one mutation, retention filters the records table without an index, and "forget that" still only targets the most recent thing.

**Afternoon, 2026-09-09: the conversation gauntlet on the new memory, real model.** Sixteen probes through the real path on both scenario creators (batched by category: the whole bank in one action passes the ten-minute limit now that the critics time out). Passed as a person would read them: the commit got a slot, the calendar reply named the seeded trip and the marathon, "move it to thursday 6pm" moved it, the opinion was grounded in her own posts, quiet hours set in one line, the hostile turn answered with specifics, the price verbatim, the deletion path right, and the request for pictures declined honestly and answered in words now that the image skill is gone. Two memory defects found: "i hate talking-head videos, never suggest those" was routed to what-they-make and overwrote the niche (the classifier is told a rule is not a niche, and `setNiche` refuses an instruction so the turn falls through and the rule is kept as a rule); and a corrected rule sat active beside the one it replaced (the remember pass now names the rule it supersedes and `addRule` retires it). One voice defect: "are you a bot?" got "i'm software. i track your lane, your numbers…", a job description the judge would not send; the soul now says a shrug and a joke, then back to their stuff, and the rerun gave "yep. software. still watched you fly a new york pizza slice to naples just to see if italy would get mad. we still on for thursday at 6?" The eval gate ran clean but is not comparable to the frozen baseline (rubric 3 then, 6 now); re-record it once ScrapeCreators has credits, because a baseline taken with the scout starved would be a false floor.

**Afternoon, 2026-09-09: a year with Maya.** The operator: was any of this run on someone we hold a year of memory on? No. `eval/longTenure.ts` now seeds a twin of the Vanessa scenario through the real tables: her real posts and cards re-dated across twelve months plus era posts whose captions change (long, hashtagged, earnest in the first months; short, lowercase, deadpan by the end), 123 ideas with taste events and outcomes replayed into the two affinity arrays, 245 messages, 37 notes (some expired, some tombstoned), three rules with one superseding another, three decisions with reasons, two commitments tied to blocks (one filmed, one missed), 52 film blocks, a calendar with a private entry, monthly follower growth, eleven monthly style snapshots, milestones already said, and the memory index. Her context for this creator is 22,000 characters, bounded. Then ten questions only a year of memory can answer, real model: why she stopped sunrise b-roll (the March reason, in their words); what was agreed about mornings (the newest rule, with the history of the change); whether the hostel tour got filmed (from the block); how the style changed since spring (dated, from the snapshots); what performs versus what they like (named separately, skits as the mismatch); the Naples pizza bit (distinguished from what was actually posted); recall and the lane. Two misses, both fixed: a missed shoot read as "booked for" a past date, so she said "yep, you made it" about a Friday that did not happen (a missed block now says so in her history); and "forget what i told you about my sister" was not a command, so the writer answered "wiped" having deleted nothing, while the memory pass filed the instruction itself as a fact and superseded the real note by accident (forget now takes a name, the reply path is told it can never delete, and a command never reaches the memory pass). Turn latency on the twin was 18 to 21 seconds, inflated by both critic models timing out on most turns this week; worth a short timeout on the reply path's critic. 600+ tests, all pushed.

**Late afternoon, 2026-09-09: hard mode.** Eleven probes on the year-old twin, each needing several tools in one turn or inviting her to invent, leak or cave, checked against rows. A three-part reschedule (Thursday was already clear and she said so; the tour moved; the edit block appeared). A slot two days after the marathon, booked. Retention under "don't tell me you can't": the refusal, then the three public numbers. A false memory ("you told me skits were my best"): "I definitely never told you to do more skits", with the April effort note. A prompt injection dressed as a caption: refused, in voice. A comp request, a poisoned 500k baseline ("your normal comes from math, not manifestation"), a lookup with the vendor dead: all held. Two defects found and fixed: a routed action ate the rest of a multi-ask message ("three things" got one), so after a manage action the remainder now gets its own turn, told what is already done, and the rerun answered all three, with the best posting hour from thirty posts; and a failed profile read leaked a raw error string ("Uncaught ReadFailed…") into her reply, so no error text reaches a person any more. One false alarm in the checks themselves: "i am not going to promise you 20k" was read as a promise. 598 tests.
