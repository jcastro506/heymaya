# Session Handoff — 2026-05-06 (Sprint 2 done, Sprint 3 next)

**Status:** Sprint 2 (deploy path consolidation + voice fix + skill bundling) — DONE.
**Next move:** Sprint 3 — Cron + heartbeat collapse + thinking budget tags.

---

## Where we are

- **Branch:** `staging` (pushed)
- **Tests:** 2704/2704 passing
- **tsc:** clean
- **Live smoke:** `npm run smoke:creator-maya-v0 -- --live --confirm` exits 0 in ~50s. Cron `enabled=true jobs=21 cron: started` observed; voice grep over `/data/workspace/*.md` returns ZERO hits.

### Read in order

1. **`docs/SPRINT_PLAN_CREATOR_MVP.md`** — locked plan. Sprint 2 marked DONE inline (with carry-forward); Sprint 3 spec is below it.
2. This file — Sprint 3 starting state + carry-forward.
3. `CLAUDE.md` — already in default context.
4. `~/.claude/projects/-Users-joshcastro-Desktop-heymaya/memory/MEMORY.md` — auto-memory. Sprint 2 done handoff at top.

---

## Sprint 2 outcome

**4 parallel slice agents + post-merge fixes:**

- **Slice A** — `assembleWorkspaceBundle.ts` is the single creator deploy path. `creatorMayaV0/backend.ts` callers point at it. 13 prose-only `creator-*` skill stubs + 8 thin-stub generators deleted from `workspaceManifest.ts`. New `generateSoulMd.ts` + `generateIdentityMd.ts` written + wired into `assembleWorkspaceBundle.ts:103`. Sprint 1 carry-forward landed: `deployMaya.ts:buildBootstrapShell()` switched from `--bind lan --port 3000` (crash-loops on v2026.4.23) to `--allow-unconfigured` loopback.
- **Slice B** — 6 voice leaks stripped (5 confirmed + 1 in `maya-calendar-classifier/script.ts:204` found by audit grep). `agents/skills/maya-platform/skill.md` → `SKILL.md` rename via `git mv` (history preserved). `tests/mayaVoice.test.ts` (17 cases) extended to grep banned terms in every workspace generator's output.
- **Slice C** — 23 creator-side `maya-*` skills bundled into `BUNDLED_SKILLS` (vs. 1 before). `metadata.openclaw` frontmatter added to each. Regen via `scripts/sync-bundled-skills.ts`. Companion test asserts byte-for-byte sync.
- **Slice D** — `pinnedClawhubSkills.ts` refreshed: `remotion-video-toolkit` dropped; `tiktok@3.0.0` kept (Growth-OS, complementary to scrapecreators read-layer); 6 ClawHub pins added (capcut, video-frames, faster-whisper, elevenlabs-transcribe, photo-text-overlay, brave-search). 3 Anthropic skills vendored faithfully from `anthropics/skills@d230a6dd` into `agents/skills/{pdf,docx,internal-comms}/` (79 files). Provenance + re-vendor command in `agents/skills/VENDOR_MANIFEST.md`.

**Post-merge fixes:**
- Re-ran `sync-bundled-skills.ts` after Slice B's voice fixes drifted skill content from the registry generated by Slice C.
- 4 deployMaya synthesis/Wave-3 tests bumped to `{ timeout: 30_000 }`. They run real model-router code paths with retries (~20s); the prior "2673/2673 passing" count from the Sprint 1 handoff was likely vitest's default 5s timeout failing them silently.
- `creator-maya-v0-fly-smoke.ts` extended with post-ready `pollCronAndVoiceReady()`: polls `cron: started` in gateway log, then voice-greps `/data/workspace/*.md`. The original `verifyMachine` was running too early (cron starts ~T+90s after gateway boot). Smoke removed `test -d /data/workspace/skills/pdf` — Anthropic vendored skills are pinned in the repo but not yet bundled to the runtime workspace (carry-forward).

**Real-world bar verified on real Fly v2026.4.23:**
- Machine booted in iad in ~50s (gateway ready at 38.5s, 5 plugins: acpx/browser/device-pair/phone-control/talk-voice)
- Workspace ships 11 root files (AGENTS, SOUL, IDENTITY, USER, MEMORY, HEARTBEAT, BOOT, TOOLS, DREAMING, standing-orders, Operations/) + 31 skills
- SOUL.md ships SEED-only anti-sycophancy frame; IDENTITY.md is `name=Maya, vibe=strategic, emoji=✨, creature=creator manager`
- Voice grep returns ZERO hits
- Cron auto-enabled with all 21 jobs from standing-orders.md (Sprint 1 win regression-checked + scaled from 1 → 21 jobs)

---

## Sprint 3 — Cron + heartbeat collapse + thinking budget tags

**Goal:** Cron is exactly 6 entries (precise timing only). HEARTBEAT.md is the proactive spine. Thinking budgets match task class.

### Scope (full spec in `docs/SPRINT_PLAN_CREATOR_MVP.md` § Sprint 3 — § lines 172-229)

**Standing-orders refactor** in `convex/agents/packs/maya/workspace/standingOrders.ts`:
- KEEP as cron (6): `morning_brief`, `evening_recap`, `weekly_content_plan`, `weekly_review`, `accountability_nudge`, `revenue_snapshot`
- MOVE to heartbeat (9, change `kind` to `"heartbeat"` or add `cronEnabled: false`): `performance_check_2h`, `daily_niche_scan`, `trend_watcher`, `comment_triage`, `competitor_watch`, `calendar_lookahead`, `industry_intel_daily`, `opportunity_scout_daily`, `collab_matchmaker_weekly`
- DELETE for MVP (6): `manager_readiness_packet_quarterly`, `algo_research_tiktok`, `algo_research_instagram`, `algo_research_youtube`, `algo_research_linkedin`, `algo_research_x`

Verify `buildCronJobsJson.ts` filters `kind === "cron"` and emits exactly 6 entries (currently emits 21 — that's the bar to drop).

**HEARTBEAT.md rewrite** in `convex/agents/packs/maya/workspace/generateHeartbeatMd.ts` — see plan § 185-201 for the 11-check ordered tick spec, idle-aware (no push 10pm-7am unless URGENT), skip-if-recent cooldowns to `mayaActionLog`. Stay under 2K char soft cap.

**Thinking budget tags** in `convex/agents/modelRouter/taskTags.ts`:
- ADD `heartbeat_tick: low`
- ADD `pre_post_scorer: high`
- ADD `underperformance_diagnoser: high`
- ADD `picture_verification: medium`
- ADD `revenue_snapshot: low`
- RAISE `weekly_content_plan` from medium → high

**Telemetry:** `mayaActionLog` captures tick-decision quality (which check fired, was it acted on, did creator engage).

### Real-world bar

- Deploy test creator (Kevin.Castro9996, +1 631-335-7603)
- Cron jobs.json on machine has exactly 6 entries
- Wait 90 min during awake hours, observe ≥2 heartbeat ticks fire (at most 1 push each)
- 10pm-7am window: zero pushes unless URGENT
- `mayaActionLog` rows stamp decisions
- Thinking budget escalation observable in `model-usage` skill output

### Definition of done

`buildCronJobsJson.ts` emits 6 entries; HEARTBEAT.md content matches audit-defined 11-check shape; 5 new task tags live; weekly_content_plan raised to high; mayaActionLog telemetry firing; live tick-and-push observed.

---

## Sprint 2 carry-forward (do NOT skip in Sprint 3)

1. **Anthropic vendored skills don't ship to runtime.** `agents/skills/{pdf,docx,internal-comms}/` are pinned in the repo (Slice D) but `BUNDLED_SKILLS` only inlines a single SKILL.md per slug. The Anthropic skills have helper scripts/examples (79 files total) that need a multi-file bundling path. Three options:
   - (a) Extend the bundle assembler to walk subtrees per slug
   - (b) Ship them via a separate ClawHub-style hydration step
   - (c) Defer until Maya needs them at runtime (likely not v0 — pdf/docx are typically run in sandboxed code-exec environments, not the Fly machine)
   
   Recommend (c) for Sprint 3 unless a specific Maya behavior demands one of them.

2. **`agents/skills/maya-platform/SKILL.md` frontmatter is `name: maya-platform-skills`** (not `maya-platform`). The deploy works because OpenClaw's skill loader uses the directory name as the slug. Worth standardizing if/when the loader ever uses the frontmatter name for resolution.

3. **`__tests__/deployMaya.test.ts` synthesis tests are slow** — 4 tests bumped to `{ timeout: 30_000 }`. They run real model-router code with retries (~20s each). Investigate whether the synthesis path can be sped up or split into faster unit tests. This was almost certainly silently failing in the "2673/2673" Sprint 1 handoff count.

---

## What's locked (operator-confirmed prior session, still active)

### Architecture
- 5 layers: OpenClaw runtime → universal skills → memory-wiki → Convex (data only) → claw-messenger channels (iMessage)
- No web UI for creators in MVP. Receipt-only landing + Stripe checkout + Clerk login.
- Generate-at-deploy for the workspace bundle. Maya owns mutation post-boot via OpenClaw's native paths.
- OpenClaw natives never reimplemented.

### Product positioning
- Coach + Manager tier names kept (operator stuck with this)
- Maya is advisory + delegated production
- Single-tier MVP for friend cohort, comped via admin flag
- iMessage-only, TikTok-only, single platform

### Integrations
- Email path = Composio Gmail (production-grade webhooks already wired)
- Calendar path = direct Google OAuth (we have read+write scopes + token refresh)
- Apple Calendar dropped from MVP

### Skills
- 6 ClawHub pins at deploy (capcut, video-frames, faster-whisper, elevenlabs-transcribe, photo-text-overlay, brave-search) + tiktok kept
- 3 Anthropic vendor: `pdf`, `docx`, `internal-comms`. Defer `xlsx`. (Vendored but not yet runtime-bundled — see carry-forward.)
- 23 creator-side custom Maya skills bundled. 11 new custom Maya skills to write across Sprints 5-7.

### Test creator (operator-provided for live smokes)
- TikTok handle: `Kevin.Castro9996`
- Phone: `+1 631-335-7603`

---

## Operator preferences refresher

- Anti-sycophancy expected. Push back when wrong. Opinions, not options.
- Native-first rule is locked. Don't reinvent OpenClaw.
- Install-first rule — search ClawHub / Anthropic / Claude Code repos before writing mechanics. Custom-author only the Maya judgment wrapper.
- Trust LLM judgment, no hardcoded rules.
- No "AI" in marketing copy — extends to runtime user-facing prose.
- Maya is one product, two pricing tiers — not two products.
- Commit + push at end of every sprint in logical chunks.
- Full autonomy to MVP — once a sprint lands clean, spawn the next automatically.
- Operator wants real-world verification per sprint, not just unit tests. Run live smokes (don't ask permission).
- Each Fly smoke iteration costs ~50s real time. Iterate efficiently. Read OpenClaw runtime source on the live machine over SSH when the schema isn't obvious.

---

## How to start next session

1. Read `docs/SPRINT_PLAN_CREATOR_MVP.md` — Sprint 2 marked DONE; read Sprint 3 spec.
2. Read this file — Sprint 3 starting state above.
3. Confirm `git status` is clean on `staging`.
4. Run `npx vitest run --reporter=dot` and `npx tsc --noEmit` — both should be clean.
5. Sprint 3 is mostly serial (refactor standing-orders → buildCronJobsJson filter → HEARTBEAT.md rewrite → thinking budget tags). Less parallelizable than Sprint 2. Spawn 1-2 slices max:
   - Slice 1: standing-orders.ts refactor + buildCronJobsJson filter + cron count test
   - Slice 2: generateHeartbeatMd.ts rewrite + thinking budget tags + telemetry
6. After all slices land + tests green: run `npm run smoke:creator-maya-v0 -- --live --confirm`, SSH-verify cron jobs.json has exactly 6 entries, observe heartbeat tick log over 90 min in waking hours. Commit + push.

---

## What MVP is NOT

- Voice calls (Twilio outbound) — defer
- Auto-send brand emails — Manager-tier autonomy gate, defer (drafts only in MVP)
- Apollo/Hunter outbound brand outreach — Studio-tier, defer
- Apple Calendar — Google-only
- Web HQ for creators — receipt-only landing, no UI
- Multi-tier billing differentiation — single comped tier for friend cohort
- Multi-platform — TikTok only
- Telegram / WhatsApp / SMS — iMessage only
- Postiz / multi-platform draft scheduling — defer (TikTok-only positioning)

When operator asks to add scope, push back to keep MVP tight unless it's a beta-blocker.
