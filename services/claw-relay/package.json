{
  "name": "claw-relay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Holds Claw Messenger's WebSocket and forwards each event, signed, to the product's /imessage/webhook (plan §23).",
  "engines": { "node": ">=22" },
  "scripts": { "start": "node index.mjs" }
}
